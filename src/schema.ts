/**
 * This file was auto-generated by openapi-typescript.
 * Do not make direct changes to the file.
 */

export interface paths {
    "/v1/companies/autocomplete": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Autocomplete suggestions
         * @description Each hit carries just enough context — company name and register reference — to pick the right result out of a dropdown. Not meant for programmatic search — use `/companies/search` for that.
         *
         *     **Free without an API key.** This is the only v1 endpoint you can call unauthenticated. Keyless access is rate limited — modestly, and by address — and is intended for trying the API out, for a search box, or for low-volume use. Send an `Authorization: Bearer <key>` header for substantially higher limits; the call still costs 0 credits either way. Respect `Retry-After` on a `429`.
         *
         *     Set `fetch_realtime=true` to also search the German company registers live and index any companies not yet known to firmendata — this is how a company founded days ago can be found before the regular register sweep reaches it. The freshly indexed companies appear in the same response. **`fetch_realtime` requires an API key** and is billed; keyless requests that set it are rejected with `401`.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["autocompleteCompanies"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/companies/search": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Advanced search
         * @description Full-text + filter search over the German commercial register. Cursor-paginated,
         *     up to 50 hits per page. `total_approx` caps at 10,000 — tighten filters for an
         *     exact count.
         *
         *     Each hit carries identity and registered seat, the register reference (court,
         *     type, number), headline financials in EUR, the company's website, and the CPV
         *     codes of any EU tenders it won.
         *
         *     Filter values are case-insensitive, several filters accept multiple values, and
         *     the financial figures need one caveat before you compute ratios with them — see
         *     the **Search** section above for all three.
         *
         *     Setting `fetch_realtime=true` additionally searches the German registers live
         *     for `q` (or `company_name`, one of which is then required) and appends any
         *     companies not yet indexed by firmendata to the first page, carrying identity and
         *     seat only. First page only — omit `cursor`.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["searchCompanies"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/companies/{eu_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Company detail
         * @description Everything you need to render a company landing page in a single call.
         *
         *     - **Identity & registration** — names, legal form, register entry, founding dates, registered address
         *     - **Financial headline** — latest-year revenue, profit, assets, liabilities, equity, employee count
         *     - **Business & industry** — trade description, products, primary WZ 2025 code with the full category tree
         *     - **Management** — register entry members (managing directors, authorised signatories, supervisory board)
         *     - **Ownership (LEI graph)** — direct parent and direct subsidiaries via GLEIF
         *     - **Contact & web presence** — primary domain, Impressum URL, primary email, and discovered social profiles
         *     - **Insolvency** — current status, court, case number, dissolution flag, most recent event date
         *     - **AI insights** — company summary, outlook, and risk + opportunity keywords in German and English
         *     - **Documents** — downloadable register documents available for the company (Aktueller Abdruck, Chronologischer Abdruck, Liste der Gesellschafter, Articles of Association — Gesellschaftsvertrag / Satzung / Statut depending on legal form, Anmeldung, Musterprotokoll); fetch a presigned URL via `/companies/{eu_id}/documents/download`
         *
         *     For multi-year history, employee time series, relationships, and Bilanz publications, call `/companies/{eu_id}/financials`.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["getCompanyDetail"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/companies/{eu_id}/documents/download": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Download a register document
         * @description Resolve one register document for the company and return a
         *     short-lived presigned download URL alongside its metadata.
         *
         *     - **`file_type`** — required. One of:
         *
         *       | `file_type` | German term | Coverage |
         *       |---|---|---|
         *       | `register_extract_current` | Aktueller Abdruck | Every Handelsregister-registered company. |
         *       | `register_extract_chronological` | Chronologischer Abdruck | Every Handelsregister-registered company. |
         *       | `shareholder_list` | Liste der Gesellschafter | GmbH and UG (haftungsbeschränkt) only. Companies founded via the simplified procedure (§2 Abs. 1a GmbHG) file no separate Liste — request `model_protocol` instead to download their founding deed. **For parsed cap-table data** (which already auto-falls-back to the Musterprotokoll), call `/companies/{eu_id}/shareholders` rather than this endpoint. |
         *       | `articles_of_association` | Gesellschaftsvertrag / Satzung / Statut (the German name varies by legal form: Gesellschaftsvertrag for GmbH/KG/OHG/GbR, Satzung for AG/eG/Verein, Statut for some specific forms) | Most companies — but not those founded via the simplified procedure, which use a Musterprotokoll (`model_protocol`) in place of a constitutional document. |
         *       | `company_registration` | Anmeldung | Any newly-registered company. |
         *       | `model_protocol` | Musterprotokoll | **Only** companies founded with the simplified procedure under §2 Abs. 1a GmbHG. Legally substitutes for both the constitutional document (Gesellschaftsvertrag / Satzung / Statut) and the Liste der Gesellschafter for those companies — it names the founders and their share allocations directly. |
         *
         *       Wire values mirror the corresponding `subscription_type` values on
         *       `POST /v1/subscriptions`, so a webhook event_type like
         *       `articles_of_association.updated` can be passed straight through here
         *       to download the file that triggered it.
         *     - **`file_id`** — optional. When omitted, falls back to the latest published
         *       file of the requested type.
         *     - **`fetch_realtime`** — optional. When `true`, fetch the requested
         *       `file_type` from the German registries before resolving the file, so the
         *       presigned URL points at the freshest version. Adds upstream latency
         *       (typically several seconds). Failures are surfaced via
         *       `freshness.realtime_fetching_status`; the response still falls back to the most
         *       recent indexed file when realtime fetching fails.
         *
         *     Returns `404` when no matching document is available — the `detail`
         *     field distinguishes whether the document is unknown to the German
         *     registries, whether the company is no longer present in the registries,
         *     or whether the registries were unreachable. The `download_url` is valid
         *     for 1 day; refetch this endpoint to obtain a new URL when it expires.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["downloadCompanyDocument"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/companies/{eu_id}/financials": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Company financials
         * @description Multi-year financial profile for one company.
         *
         *     - **Aggregated summary** — latest-year revenue, profit, assets, liabilities, and equity
         *     - **Multi-year history** — annual top-line metrics plus structured P&L, AKTIVA, and PASSIVA rows, with AI-reconstructed P&L for small filings that lack a structured GuV
         *     - **Employee time series** — total, full-time, and part-time headcount per year
         *     - **Group relationships** — parent company and known subsidiaries sourced from consolidated filings
         *     - **Filed Bilanzen** — list of Jahresabschluss and Konzernabschluss filings with type and publication date (PDF downloads are not exposed in v1)
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["getCompanyFinancials"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/companies/{eu_id}/history": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Chronological register history
         * @description Aggregated chronological register-entry history for a company.
         *
         *     For each kind of change — company name (`firma`), seat (`sitz`),
         *     capital (`kapital`), board members, business purpose, etc. — the
         *     endpoint returns the full timeline of entries that affected it.
         *     `current_board` is a derived snapshot of who currently sits on the
         *     management board.
         *
         *     - **`fetch_realtime`** — optional. When `true`, refresh the
         *       underlying register history from the German registries before
         *       responding. Adds upstream latency (typically several seconds).
         *       Failures are surfaced via `freshness.realtime_fetching_status`; the response
         *       still falls back to the most recent indexed data when a refresh
         *       fails.
         *
         *     A 404 is returned when no chronological register history has ever
         *     been recorded for this company — some Vereine and Genossenschaften
         *     fall outside the chronological-register coverage at Handelsregister.
         *     The same data backs the `history` webhook subscription type, so the
         *     shape returned here is exactly the shape downstream consumers fetch
         *     when responding to a `history.updated` webhook.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["getCompanyHistory"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/companies/{eu_id}/shareholders": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Shareholder cap table
         * @description Latest aggregated Gesellschafterliste (cap table) for a GmbH or UG.
         *
         *     - **Coverage status** — whether a cap table is `available`, `not_applicable` (the legal form does not file Gesellschafterlisten), or `not_filed` (expected but missing)
         *     - **Shareholders** — each participant with share count, percentage of capital, and share class
         *     - **As-of metadata** — filing date of the aggregated list
         *     - **`fetch_realtime`** — optional. When `true`, scrape the latest Liste der
         *       Gesellschafter from the German registries and re-parse it before returning.
         *       Adds upstream latency (typically several seconds). Scraping or parsing
         *       failures are surfaced via `freshness.realtime_fetching_status`; the response still
         *       falls back to the most recent indexed cap table when a refresh fails.
         *
         *     Historical snapshots and cross-filing diffs are reserved for a later release.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["getCompanyShareholders"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/companies/{eu_id}/ubo": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Ultimate beneficial owner (UBO) report
         * @description Walk the company's ownership chain and identify the natural persons
         *     who ultimately benefit from it.
         *
         *     For every German GmbH/UG encountered in the chain, the latest filed
         *     Gesellschafterliste is read; recursion stops at natural persons,
         *     foreign organisations, or any organisation without a German
         *     Gesellschafterliste obligation. Each branch carries its direct share
         *     percentage. Beneficial-owner attribution follows the German
         *     "all-or-nothing" rule: each link in the chain is independently checked
         *     against the 25 % control threshold (covering both >50 % majority
         *     control and >25 % Sperrminorität under the BVA's post-TraFinG
         *     reading of §3 Abs. 2 Nr. 3 GwG). The share attributed to a UBO is
         *     the share at the final hop into the root — the actual capital stake
         *     the UBO ultimately controls, not the multiplicative product.
         *
         *     - **Graph** — every node and directed `source → target` edge
         *       traversed during the walk. Use it to render an ownership tree or to
         *       audit which branches stop short.
         *     - **Beneficial owners** — natural persons attributed more than 25 %
         *       of the root via direct holdings, controlled chains, or the sum of
         *       both, sorted descending. Empty when no person crosses the threshold
         *       (e.g. ownership diluted across many parties or the chain ends in
         *       unresolved organisations). Fictional UBO under §3 Abs. 2 S. 5 GwG
         *       is not surfaced.
         *     - **Potential beneficial owners** — unresolved organisations whose
         *       summed effective share at the root crosses 25 % under the same
         *       all-or-nothing rule. The natural person(s) controlling each entity
         *       are candidate UBOs that we couldn't trace ourselves. Same-entity
         *       dedup across multiple chain positions is by `(name, registered_
         *       seat, country_code)`, so an org appearing twice with 15 % + 15 %
         *       surfaces as one entry at 30 %.
         *     - **Coverage** — `coverage.status` distinguishes four states:
         *       `available` (chain fully resolved, OR remaining unresolved
         *       branches sum to ≤25 % effective share and cannot hide a UBO);
         *       `partial` (an unresolved branch could hide a beneficial owner —
         *       read `potential_beneficial_owners` and `coverage.reason`);
         *       `not_filed` (no cap table on file); `not_applicable` (legal form
         *       has no Gesellschafterliste obligation).
         *     - **Completeness** — `has_unresolved_branches` flags any non-root
         *       unresolved node in the graph; whether that compromises the answer
         *       is encoded in `coverage.status` (`partial` if so, otherwise
         *       `available` with `coverage.reason` explaining why the unresolved
         *       branches are immaterial).
         *     - **`fetch_realtime`** — optional. When `true`, refetch the latest
         *       Liste der Gesellschafter for every German GmbH/UG in the chain
         *       before building the graph. Adds noticeable latency (several seconds
         *       per company in the chain). Failures are surfaced via
         *       `freshness.realtime_fetching_status`; the graph still falls back to the most
         *       recent indexed cap tables when a refresh fails.
         *
         *     **Edge cases worth knowing:**
         *
         *     - **Control passes through 100 %, not the chain product.** A holds
         *       60 % of H, H holds 30 % of root → A is a UBO with
         *       `effective_share_percent = 30`, not `18`. Each link is checked
         *       independently against the 25 % threshold.
         *     - **Sperrminorität counts as control.** A 26 % blocking minority at
         *       any intermediate level continues the chain; the 50 % majority bar
         *       only matters as the higher of the two ways to qualify.
         *     - **Sub-threshold links break the chain.** A holds 15 % of H, H
         *       holds 30 % of root → A is **not** attributed root ownership; H
         *       isn't controlled by A.
         *     - **Direct positions aggregate even below 25 %.** A person holding
         *       15 % + 15 % directly in the root is a UBO at 30 %.
         *     - **Multiple controlled branches sum.** A controls X and Y; X holds
         *       30 %, Y holds 40 % of root → A's `effective_share_percent = 70`
         *       (each branch is independently a control link, and the root-edge
         *       shares add up).
         *
         *     Designed for KYC workflows where the freshest possible chain matters.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["getCompanyUbo"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/lists": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Your company lists
         * @description Every company list the authenticated caller owns, newest first.
         *
         *     Returns each list's id, name, company count and whether change alerts are
         *     enabled — enough to pick one and fetch its companies with
         *     `GET /v1/lists/{list_id}`. Lists are created and edited in the web app.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["getLists"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/lists/{list_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Companies on a list
         * @description One page of companies from one of the caller's lists.
         *
         *     - **Rows** match `GET /v1/companies/search` hits exactly, so both surfaces
         *       deserialize into the same type
         *     - **`sort`** orders the whole list before the page is cut, so paging never
         *       reshuffles rows. Omit it for the order companies were added in
         *     - **Pagination** is the standard opaque cursor; `pagination.total_approx` is
         *       the list's exact size
         *
         *     A list id that does not exist, or belongs to someone else, returns `404` —
         *     the two are deliberately indistinguishable.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["getList"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/subscriptions": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List subscriptions
         * @description Returns every subscription owned by the calling user, newest first.
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["listSubscriptions"];
        put?: never;
        /**
         * Create subscription
         * @description Create a webhook, email, or async (poll-only) subscription for a single
         *     company.
         *
         *     ## Async (poll-only) subscriptions
         *
         *     Pass `"notification_type": null` (and omit both `webhook_url` and
         *     `recipient_email`) to create a poll-only subscription. firmendata
         *     performs the same change detection as for webhook/email, but instead of
         *     calling out to your infrastructure on each change it records the
         *     rendered payload as a `subscription_event`. Retrieve change events
         *     afterwards via `GET /v1/subscriptions/{subscription_id}/events`,
         *     where `subscription_id` is the value returned in this response (the
         *     "job id" the response carries). `webhook_secret` is `null` for async
         *     subscriptions — there is no outbound HTTP to sign.
         *
         *     ## Webhook signature verification
         *
         *     firmendata signs **every** outgoing webhook request with HMAC-SHA256
         *     so receivers can verify the request actually came from firmendata.
         *     Verify on every incoming POST:
         *
         *     * `X-Firmendata-Signature: sha256=<hex>` — HMAC-SHA256 of
         *       `f"{X-Firmendata-Timestamp}.{raw_body}"` keyed by the
         *       `webhook_secret` returned in this response. Use a constant-time
         *       comparison.
         *     * `X-Firmendata-Timestamp: <unix-seconds>` — reject values older than
         *       5 minutes to defeat replay attacks.
         *     * `X-Firmendata-Delivery-Id: <uuid>` — idempotency key; safe to dedupe
         *       on. firmendata may re-POST the same delivery id after a transient
         *       receiver error.
         *     * `X-Firmendata-Event-Type: <subscription_type>.updated` — e.g.
         *       `details.updated`, `doc_articles_of_association.updated`.
         *
         *     The response carries a one-time **`webhook_secret`**. Store it now —
         *     it is **not** recoverable later. To rotate, delete the subscription
         *     and re-create it.
         *
         *     ## Defaults
         *
         *     * `mode` → `recurring` — see `mode` for the `one_time` alternative.
         *     * `notification_type` → `null` (async, poll-only) — see `notification_type` for the `webhook` / `email` alternatives.
         *     * `cadence` → `daily`.
         *     * `language` → `en` (affects email rendering only).
         *
         *     ## Email-mode rendering
         *
         *     When `notification_type=email`, the recipient's inbox shows:
         *
         *     * **Body table** with the company name, company id (`eu_id`),
         *       notification type, publish date, and — for `doc_*` subscriptions —
         *       the document type and file id. No authenticated `/v1/...` API URLs
         *       are rendered (email recipients carry no bearer token); a single
         *       public **"View company on FirmenData"** button links to
         *       `https://firmendata.com/{language}/company/{eu_id}`.
         *     * **PDF attachment** (only for `doc_*` subscriptions) — the actual
         *       register document (Articles of Association / Anmeldung /
         *       Musterprotokoll / Liste der Gesellschafter) attached as a
         *       `application/pdf` MIME part. Recipients can save / forward / parse
         *       the file straight from their inbox.
         *
         *     Parsed-data subscriptions (`details`, `history`, `shareholders`,
         *     `ubo`) deliver the body table only — the structured JSON itself
         *     remains accessible via the v1 API for authenticated clients, and
         *     the email recipient gets a human summary.
         *
         *     ## Cadence semantics
         *
         *     `cadence` controls how often you wish to receive notifications. A
         *     notification is only delivered when there is an actual change since
         *     the previous successful delivery — no update means no notification.
         *     `immediately` delivers an update as soon as it is detected, with a
         *     service-level objective of within 1 hour of publication.
         *     `daily` / `weekly` / `monthly` batch the check at the named cadence.
         *
         *     For `recurring` subscriptions, the **first** delivery is a snapshot
         *     of the current state; after that, deliveries only fire on actual
         *     updates.
         *
         *     For `one_time` subscriptions, `cadence` is the retry spacing while
         *     we wait for the data to become available; the subscription stops on
         *     the first successful delivery.
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        post: operations["createSubscription"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/subscriptions/events/{event_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get event
         * @description Returns the full event including the JSON body firmendata POSTed and the `request_signature` / `request_timestamp` needed to re-verify the HMAC offline.
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["getEvent"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/subscriptions/events/{event_id}/resend": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Resend event
         * @description Re-sends a previously delivered webhook event to the subscription's current `webhook_url`. The body is cloned from the stored event verbatim; only `event_id` and `delivered_at` are refreshed so the receiver sees a distinct delivery it can dedupe on. Records a new event row — the original is left untouched.
         *
         *     Only valid for webhook subscriptions whose original event has a stored `request_body`. Email events, async (poll-only) events, and rows that predate request-body persistence return 422 — there's nothing to re-POST.
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        post: operations["resendEvent"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/subscriptions/test": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        get?: never;
        put?: never;
        /**
         * Test delivery
         * @description Send one **real** sample notification — webhook **or** email — using
         *     the same rendering, signing, and delivery path as production. **No
         *     subscription is created**, no DB row is written, and no scrape runs.
         *     Safe and cheap to call repeatedly from your CI / integration tests.
         *
         *     Pick the transport via `notification_type`:
         *
         *     * **`webhook`** — POSTs a signed JSON body to `webhook_url`. The
         *       request is always signed with the documented public test secret
         *       `firmendata_test_secret_do_not_use_in_production`. The HMAC recipe
         *       is identical to production, so any verification code that matches
         *       with this constant will also match a real per-subscription secret.
         *       Hard-code it in your unit tests; never reuse it for real
         *       subscriptions.
         *     * **`email`** — composes the bilingual change-notification email
         *       (subject + HTML body) and SMTPs it to `recipient_email`. Subject
         *       is prefixed with `[TEST]`; the body carries an explicit "this is a
         *       test" banner so a human reading the inbox can't mistake it for a
         *       live update. For `doc_*` subscription types the response also
         *       attaches a tiny on-the-fly **sample PDF** so the customer's
         *       attachment-handling pipeline (parse Content-Type, save to disk,
         *       forward to S3, …) can be exercised end to end. In production the
         *       attachment is the real register PDF.
         *
         *     No retries — this is a fail-fast probe. The receiver's status, error
         *     detail, and the exact bytes / headers / subject / body firmendata
         *     sent are all returned, so you can diff them against your verification
         *     routine offline if your endpoint isn't yet live.
         *
         *     `subscription_type` controls the payload shape, summary copy, and —
         *     for `doc_*` types — the embedded download URL / file metadata block,
         *     identical to what a real notification carries. `eu_id` is embedded in
         *     the payload but does **not** need to be known to firmendata.
         *
         *     ## Webhook payload faithfulness
         *
         *     Top-level `publish_date` and `links` are present on every delivery.
         *     The per-type `data` block matches a production change delivery for
         *     the same `subscription_type`:
         *
         *     * Parsed-data types (`details`, `history`, `shareholders`, `ubo`)
         *       deliver the **same response shape as the corresponding
         *       `GET /v1/companies/{eu_id}[/...]` endpoint**. When the
         *       `eu_id` you pass is known to firmendata the test endpoint embeds the
         *       real current data; when it does not, `data` is a sparse placeholder
         *       containing only `{eu_id}` — the field set is what varies, the schema
         *       does not.
         *     * `doc_*` types deliver `{url, expires_at, file_id, file_type,
         *       fetched_at}` directly under `data`. In test mode the `url` points at
         *       the v1 documents endpoint (clearly identifiable as a test artifact);
         *       in production it is a 7-day presigned URL to the real document.
         *
         *     The only top-level difference between test and production is an extra
         *     `_meta: {"test": true}` key — receivers that ignore unknown top-level
         *     keys (recommended) need no test branch at all. The `subscription_id`
         *     is a freshly-minted UUID, not a real id; treat it as opaque.
         *
         *     ## Integration-test scenarios
         *
         *     Webhook only — pick via `delivery_scenario`:
         *
         *     * `success` (default) — single signed POST. Receiver should return 2xx.
         *     * `retry_delivery` — two POSTs back-to-back with byte-identical body,
         *       signature, timestamp, and `X-Firmendata-Delivery-Id`. Mirrors how
         *       production retries look after a 5xx so you can verify your
         *       idempotent dedupe.
         *     * `bad_signature` — single POST signed with a wrong secret. Your
         *       endpoint SHOULD reject with 4xx; that is the test passing.
         *     * `stale_timestamp` — single POST whose `X-Firmendata-Timestamp` is
         *       10 minutes in the past. Your endpoint SHOULD reject with 4xx; that
         *       is the test passing.
         *
         *     Per-attempt detail (status, signature, timestamp, response excerpt)
         *     is returned in `attempts[]` regardless of scenario.
         *
         *     ## Sample receiver
         *
         *     A minimal listener that verifies the signature and prints the body —
         *     drop into your test harness, point your `webhook_url` at it, and call
         *     this endpoint to drive it.
         *
         *     ### Python (Flask)
         *
         *     ```python
         *     # pip install flask
         *     import hashlib
         *     import hmac
         *     import json
         *     from flask import Flask, abort, request
         *
         *     WEBHOOK_SECRET = "firmendata_test_secret_do_not_use_in_production"
         *     MAX_TIMESTAMP_SKEW_SECONDS = 5 * 60
         *
         *     app = Flask(__name__)
         *
         *
         *     @app.post("/firmendata/webhook")
         *     def receive_webhook():
         *         raw = request.get_data()
         *         sig = request.headers.get("X-Firmendata-Signature", "")
         *         ts = request.headers.get("X-Firmendata-Timestamp", "")
         *
         *         expected = "sha256=" + hmac.new(
         *             WEBHOOK_SECRET.encode(),
         *             f"{ts}.".encode() + raw,
         *             hashlib.sha256,
         *         ).hexdigest()
         *         if not hmac.compare_digest(sig, expected):
         *             abort(401)
         *
         *         # Replay protection — reject anything older than 5 minutes.
         *         import time
         *         if abs(int(time.time()) - int(ts or 0)) > MAX_TIMESTAMP_SKEW_SECONDS:
         *             abort(401)
         *
         *         payload = json.loads(raw)
         *         print(
         *             f"[{payload['event_type']}] eu_id={payload['eu_id']} "
         *             f"delivery_id={request.headers['X-Firmendata-Delivery-Id']}"
         *         )
         *         print(json.dumps(payload, indent=2))
         *         return ("", 200)
         *
         *
         *     if __name__ == "__main__":
         *         app.run(port=8080)
         *     ```
         *
         *     ### Node.js (Express)
         *
         *     ```js
         *     // npm install express
         *     import crypto from "node:crypto";
         *     import express from "express";
         *
         *     const WEBHOOK_SECRET = "firmendata_test_secret_do_not_use_in_production";
         *     const MAX_TIMESTAMP_SKEW_SECONDS = 5 * 60;
         *
         *     const app = express();
         *
         *     app.post(
         *       "/firmendata/webhook",
         *       express.raw({ type: "application/json" }),
         *       (req, res) => {
         *         const sig = req.header("X-Firmendata-Signature") ?? "";
         *         const ts = req.header("X-Firmendata-Timestamp") ?? "";
         *
         *         const expected =
         *           "sha256=" +
         *           crypto
         *             .createHmac("sha256", WEBHOOK_SECRET)
         *             .update(`${ts}.${req.body.toString()}`)
         *             .digest("hex");
         *         const sigBuf = Buffer.from(sig);
         *         const expBuf = Buffer.from(expected);
         *         if (
         *           sigBuf.length !== expBuf.length ||
         *           !crypto.timingSafeEqual(sigBuf, expBuf)
         *         ) {
         *           return res.status(401).end();
         *         }
         *
         *         if (Math.abs(Math.floor(Date.now() / 1000) - Number(ts || 0)) >
         *             MAX_TIMESTAMP_SKEW_SECONDS) {
         *           return res.status(401).end();
         *         }
         *
         *         const payload = JSON.parse(req.body.toString());
         *         console.log(
         *           `[${payload.event_type}] eu_id=${payload.eu_id} ` +
         *             `delivery_id=${req.header("X-Firmendata-Delivery-Id")}`
         *         );
         *         console.log(JSON.stringify(payload, null, 2));
         *         res.status(200).end();
         *       }
         *     );
         *
         *     app.listen(8080);
         *     ```
         *
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        post: operations["testDelivery"];
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/subscriptions/{subscription_id}": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * Get subscription
         * @description Returns the subscription plus the most recent delivery attempt embedded in `last_delivery`. Use this endpoint to poll the status of a `one_time` subscription while you wait for the data to become available.
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["getSubscription"];
        put?: never;
        post?: never;
        /**
         * Delete subscription
         * @description Permanently deletes the subscription and its delivery history. Returns `204 No Content` on success and `404` when the subscription does not exist or belongs to another user.
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        delete: operations["deleteSubscription"];
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
    "/v1/subscriptions/{subscription_id}/events": {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        /**
         * List events
         * @description Returns delivery attempts for `subscription_id`, newest first. Each event includes the exact JSON body firmendata POSTed plus the signing material, so customers can render a Stripe-style history view and re-verify signatures offline.
         *
         *     ## Pricing
         *
         *     See the [pricing page](https://firmendata.com/en/plans) for this endpoint's credit cost.
         */
        get: operations["listEvents"];
        put?: never;
        post?: never;
        delete?: never;
        options?: never;
        head?: never;
        patch?: never;
        trace?: never;
    };
}
export type webhooks = Record<string, never>;
export interface components {
    schemas: {
        /**
         * Address
         * @description A postal address with optional geocoding.
         */
        Address: {
            /**
             * City
             * @description City or municipality.
             */
            city?: string | null;
            /**
             * Country Code
             * @description ISO 3166-1 alpha-2 country code. Defaults to `DE`.
             * @default DE
             */
            country_code: string;
            /**
             * Federal State
             * @description German federal state (Bundesland).
             */
            federal_state?: string | null;
            /**
             * Formatted Display
             * @description Pre-formatted single-line address suitable for display.
             */
            formatted_display?: string | null;
            /**
             * House Number
             * @description House number, including any suffix.
             */
            house_number?: string | null;
            /**
             * Latitude
             * @description Latitude in decimal degrees (WGS-84). `null` when geocoding is unavailable.
             */
            latitude?: number | null;
            /**
             * Longitude
             * @description Longitude in decimal degrees (WGS-84). `null` when geocoding is unavailable.
             */
            longitude?: number | null;
            /**
             * Object
             * @description Discriminator. Always `address`.
             * @default address
             * @constant
             */
            object: "address";
            /**
             * Postal Code
             * @description Postal code (PLZ).
             */
            postal_code?: string | null;
            /**
             * Street
             * @description Street name.
             */
            street?: string | null;
        };
        /**
         * AiInsightsBlock
         * @description AI-generated insights. Populated only when `expand=ai_insights`.
         */
        AiInsightsBlock: {
            /**
             * Object
             * @description Discriminator. Always `ai_insights`.
             * @default ai_insights
             * @constant
             */
            object: "ai_insights";
            /** @description Forward-looking outlook, localized. */
            outlook?: components["schemas"]["LocalizedText"] | null;
            /** @description One-paragraph company summary, localized. */
            summary?: components["schemas"]["LocalizedText"] | null;
        };
        /**
         * AutocompleteHit
         * @description One autocomplete suggestion.
         */
        AutocompleteHit: {
            /**
             * Display Name
             * @description Best human-readable name to render in the suggestion list.
             */
            display_name: string;
            /**
             * Eu Id
             * @description firmendata company identifier.
             */
            eu_id: string;
            /**
             * Legal Name
             * @description Full legal name as filed.
             */
            legal_name: string;
            /**
             * Object
             * @description Discriminator. Always `autocomplete_hit`.
             * @default autocomplete_hit
             * @constant
             */
            object: "autocomplete_hit";
            /**
             * Register Court
             * @description Name of the registering court (Registergericht).
             */
            register_court?: string | null;
            /**
             * Register Number
             * @description Register entry number (digits only). Combine with `register_type` for a full reference.
             * @example 12345
             */
            register_number?: number | null;
            /**
             * Register Type
             * @description Register type code (e.g. `HRB`, `HRA`, `GnR`, `PR`, `VR`).
             */
            register_type?: string | null;
        };
        /**
         * AutocompleteResponse
         * @description Response envelope for `GET /v1/companies/autocomplete`.
         */
        AutocompleteResponse: {
            /**
             * Data
             * @description Suggestion hits ordered by match relevance.
             */
            data?: components["schemas"]["AutocompleteHit"][];
            /**
             * Filters Echo
             * @description The effective filter values applied to this query, echoed back for client convenience.
             */
            filters_echo?: {
                [key: string]: unknown;
            };
            /** @description Outcome of the `fetch_realtime` register search for this request. `last_checked_at` is always `null` here — the suggestions span many companies, so no single slice timestamp applies. */
            freshness?: components["schemas"]["Freshness"];
            /**
             * Object
             * @description Discriminator. Always `list`.
             * @default list
             * @constant
             */
            object: "list";
            /** @description Pagination metadata for this page. */
            pagination?: components["schemas"]["PaginationInfo"];
        };
        /**
         * BeneficialOwner
         * @description A natural person who qualifies as a beneficial owner of the
         *     root under §3 GwG (German "all-or-nothing" attribution: each link
         *     in the ownership chain is checked independently against the 25 %
         *     threshold; Sperrminorität above 25 % counts as control under the
         *     BVA's post-TraFinG reading of §3 Abs. 2 Nr. 3 GwG).
         */
        BeneficialOwner: {
            /**
             * Effective Share Percent
             * @description Capital share of the root attributable to this person. For direct holdings, the sum of their direct positions. For indirect holdings, the share at the final hop into the root (the company directly held by the root that this person ultimately controls); when reached via several distinct branches, the sum of those branches' root-edge shares. **Not** a multiplicative product — German UBO law treats every controlling link as 100 % pass-through. Always strictly greater than 25.
             */
            effective_share_percent: number;
            /**
             * Node Id
             * @description `UboNode.id` of the beneficial owner — always points at a `person` node.
             */
            node_id: string;
            /**
             * Object
             * @description Discriminator. Always `beneficial_owner`.
             * @default beneficial_owner
             * @constant
             */
            object: "beneficial_owner";
            /** @description Natural-person details. */
            person: components["schemas"]["Person"];
        };
        /**
         * BilanzAttachment
         * @description Reference to the filed Bilanz PDF.
         */
        BilanzAttachment: {
            /**
             * Fetched At
             * @description When firmendata last fetched the file.
             */
            fetched_at?: string | null;
            /**
             * File Id
             * @description firmendata file identifier.
             */
            file_id: string;
            /**
             * File Name
             * @description Original file name as published.
             */
            file_name?: string | null;
            /**
             * File Size Bytes
             * @description File size in bytes, when known.
             */
            file_size_bytes?: number | null;
            /**
             * File Type
             * @description Attachment kind (`annual_report_pdf`, `consolidated_report_pdf`, `combined_report_pdf`).
             */
            file_type?: string | null;
            /**
             * Object
             * @description Discriminator. Always `bilanz_attachment`.
             * @default bilanz_attachment
             * @constant
             */
            object: "bilanz_attachment";
            /**
             * Public Name
             * @description Display name of the file.
             */
            public_name?: string | null;
        };
        /**
         * Bundesland
         * @enum {string}
         */
        Bundesland: "Baden-Württemberg" | "Bayern" | "Berlin" | "Brandenburg" | "Bremen" | "Hamburg" | "Hessen" | "Mecklenburg-Vorpommern" | "Niedersachsen" | "Nordrhein-Westfalen" | "Rheinland-Pfalz" | "Saarland" | "Sachsen" | "Sachsen-Anhalt" | "Schleswig-Holstein" | "Thüringen";
        /**
         * BusinessBlock
         * @description Business block: products, segments, mined keywords.
         */
        BusinessBlock: {
            /**
             * Business Branches
             * @description Reported business segments and their revenue split.
             */
            business_branches?: components["schemas"]["BusinessBranch"][];
            /**
             * Object
             * @description Discriminator. Always `company_business`.
             * @default company_business
             * @constant
             */
            object: "company_business";
            /**
             * Opportunity Keywords
             * @description Opportunity indicators mined from management reports.
             */
            opportunity_keywords?: string[];
            /**
             * Products
             * @description Products and services mined from filings.
             */
            products?: string[];
            /**
             * Risk Keywords
             * @description Risk indicators mined from management reports.
             */
            risk_keywords?: string[];
        };
        /**
         * BusinessBranch
         * @description One business branch / segment with optional revenue split.
         */
        BusinessBranch: {
            /**
             * Name
             * @description Branch or segment name.
             */
            name: string;
            /**
             * Object
             * @description Discriminator. Always `business_branch`.
             * @default business_branch
             * @constant
             */
            object: "business_branch";
            /**
             * Parent Name
             * @description Parent branch name, when nested.
             */
            parent_name?: string | null;
            /**
             * Percentage Of Total
             * @description Branch share of total revenue (0–100).
             */
            percentage_of_total?: number | null;
            /**
             * Revenue
             * @description Revenue attributed to this branch in `revenue_unit`.
             */
            revenue?: number | null;
            /**
             * Revenue Unit
             * @description Unit/scale label for `revenue`.
             */
            revenue_unit?: string | null;
        };
        /**
         * CompanyDetail
         * @description Hero detail profile returned by `GET /v1/companies/{eu_id}`.
         *
         *     Every block is populated on every response — no `expand[]` opt-in.
         *     Heavy historic data (financial history, employee history, parent/
         *     subsidiary relationships) lives on the separate
         *     `GET /v1/companies/{eu_id}/financials` endpoint.
         */
        CompanyDetail: {
            /** @description Registered seat address. */
            address?: components["schemas"]["Address"] | null;
            /** @description AI-generated summary and outlook, localized. `null` when no AI insights are available for this company. */
            ai_insights?: components["schemas"]["AiInsightsBlock"] | null;
            /** @description Business profile (products, segments, keywords). `null` when no products, segments, or mined keywords are available. */
            business_info?: components["schemas"]["BusinessBlock"] | null;
            /**
             * Business Purpose
             * @description Company purpose (Gegenstand des Unternehmens, German as filed).
             */
            business_purpose?: string | null;
            /** @description Contact and web presence (domain, logo, social profiles, LinkedIn enrichment). */
            contact: components["schemas"]["ContactBlock"];
            /**
             * Display Name
             * @description Best human-readable name to use in UIs.
             */
            display_name: string;
            /**
             * Documents
             * @description Downloadable register documents available for this company (Aktueller Abdruck, Chronologischer Abdruck, Liste der Gesellschafter, Articles of Association — Gesellschaftsvertrag / Satzung / Statut depending on legal form, Anmeldung, Musterprotokoll). Use `GET /companies/{eu_id}/documents/download` to obtain a presigned URL for any entry.
             */
            documents?: components["schemas"]["CompanyDocument"][];
            /**
             * Elf Code
             * @description ISO 20275 Entity Legal Form (ELF) code, when mapped.
             * @example 6QQB
             */
            elf_code?: string | null;
            /**
             * Eu Id
             * @description firmendata company identifier.
             */
            eu_id: string;
            /** @description Headline figures for the latest reported fiscal year. `null` when no financial filings are available. For the full multi-year history, call `GET /v1/companies/{eu_id}/financials`. */
            financial_summary?: components["schemas"]["FinancialSummaryBlock"] | null;
            /**
             * First Register Entry At
             * @description Date of the company's first observable register entry.
             */
            first_register_entry_at?: string | null;
            /** @description Founder, family-involvement and succession assessment derived from the register's board history. `null` when the company could not be assessed at all. */
            founder_profile?: components["schemas"]["FounderProfileBlock"] | null;
            /**
             * Founding Document Date
             * @description Date of the founding document (Gründungsdokument), as filed in the register.
             */
            founding_document_date?: string | null;
            /** @description How current the register snapshot in this response is. `last_checked_at` is the timestamp at which the company's structured register snapshot (SI) was last successfully fetched; `realtime_fetching_status` reports the outcome of the realtime attempt for this request. The chronological register history has its own freshness object served by `/v1/companies/{eu_id}/history`. */
            freshness: components["schemas"]["Freshness"];
            /** @description Industry classification (NACE codes). */
            industry_categorization: components["schemas"]["IndustryBlock"];
            /** @description Insolvency, Auflösung and Liquidation snapshot. `null` when the company has no such event on record and is not dissolved. */
            insolvency?: components["schemas"]["InsolvencyBlock"] | null;
            /**
             * Latest Register Entry Date
             * @description Date of the most recent register entry observed for this company (XJustiz `letzteEintragung`). `null` when no register entry has yet been observed for this company.
             */
            latest_register_entry_date?: string | null;
            /**
             * Legal Form
             * @description German legal-form short name (Rechtsform), e.g. `GmbH`, `AG`, `KG`.
             * @example GmbH
             */
            legal_form?: string | null;
            /**
             * Legal Name
             * @description Full legal name as filed.
             */
            legal_name?: string | null;
            /** @description LEI-based ownership graph — parent and direct children when the company has an LEI. `null` when the company has no LEI and no LEI-graph relations are available. */
            lei_graph?: components["schemas"]["LeiGraphBlock"] | null;
            /**
             * Management
             * @description Management members from the register entry (managing directors, authorised signatories, supervisory board). Excludes non-management roles like Rechtsträger and Registergericht.
             */
            management?: components["schemas"]["Member"][];
            /**
             * Object
             * @description Discriminator. Always `company`.
             * @default company
             * @constant
             */
            object: "company";
            /**
             * Register Court
             * @description Name of the registering court (Registergericht).
             */
            register_court?: string | null;
            /**
             * Register Number
             * @description Register entry number (digits only). Combine with `register_type` for a full reference.
             * @example 12345
             */
            register_number?: number | null;
            /**
             * Register Type
             * @description Register type code (e.g. `HRB`, `HRA`, `GnR`, `PR`, `VR`).
             */
            register_type?: string | null;
            /**
             * Registered Seat
             * @description Registered seat as filed (German, may include district).
             */
            registered_seat?: string | null;
            /** @description Currently registered share capital (Stammkapital/Grundkapital). */
            share_capital?: components["schemas"]["Money"] | null;
            /**
             * Status
             * @description Derived lifecycle status, resolved from the merged Strukturierte-Inhalte, Chronologisches-Datenblatt and Insolvenzbekanntmachungen timelines — the most recent event wins regardless of which source recorded it.
             *
             *     `deleted` — company has been removed from the German registries.
             *     `insolvent` — insolvency proceedings are open or preliminary measures are in force.
             *     `in_liquidation` — the company is dissolved (Auflösung, whether by shareholder resolution or as a consequence of insolvency) and a Liquidator is in office.
             *     `dissolved` — dissolved with no Liquidator in office, or an insolvency proceeding that has been lifted (Aufhebung des Verfahrens).
             *     `active` — default for everything else.
             * @default active
             * @enum {string}
             */
            status: "active" | "in_liquidation" | "insolvent" | "dissolved" | "deleted";
            /**
             * Ust Id Nr
             * @description VAT identification number (Umsatzsteuer-Identifikationsnummer).
             */
            ust_id_nr?: string | null;
            /**
             * Wirtschafts Id Nr
             * @description Wirtschafts-Identifikationsnummer (W-IdNr.) for the main establishment, derived from `ust_id_nr` by appending `-00001` (e.g. `DE123456789-00001`). `null` when no USt-IdNr is known.
             */
            wirtschafts_id_nr?: string | null;
        };
        /**
         * CompanyDocument
         * @description One downloadable register document available for the company.
         */
        CompanyDocument: {
            /**
             * File Id
             * @description Stable file identifier. Pass to `/documents/download` to obtain a presigned URL.
             */
            file_id: string;
            /**
             * File Name De
             * @description German display name composed as `"<type label> vom <date>"` (e.g. `Liste der Gesellschafter vom 2026-02-17`).
             */
            file_name_de: string;
            /**
             * File Name En
             * @description English display name composed as `"<type label> from <date>"` (e.g. `Shareholder List from 2026-02-17`).
             */
            file_name_en: string;
            /**
             * Object
             * @description Discriminator. Always `company_document`.
             * @default company_document
             * @constant
             */
            object: "company_document";
            /**
             * Published At
             * @description Publication date as filed in the register. The `register_extract_current` and `register_extract_chronological` document types are not stamped with a publication date by the registry — for those, this falls back to the date portion of `updated_at`.
             */
            published_at?: string | null;
            /**
             * Type
             * @description Kind of register document.
             * @enum {string}
             */
            type: "register_extract_current" | "register_extract_chronological" | "shareholder_list" | "articles_of_association" | "company_registration" | "model_protocol";
            /**
             * Type Label De
             * @description German type name (e.g. `Aktueller Abdruck`, `Chronologischer Abdruck`, `Liste der Gesellschafter`, `Gesellschaftsvertrag` / `Satzung` / `Statut` (the term varies by legal form), `Anmeldung`, `Musterprotokoll`).
             */
            type_label_de: string;
            /**
             * Type Label En
             * @description English type name (e.g. `Current Register Extract`, `Chronological Register Extract`, `Shareholder List`, `Articles of Association`, `Company Registration`, `Model Protocol`).
             */
            type_label_en: string;
            /**
             * Updated At
             * Format: date-time
             * @description When firmendata last fetched this document.
             */
            updated_at: string;
        };
        /**
         * CompanyDocumentDownload
         * @description Document metadata plus a short-lived presigned download URL.
         */
        CompanyDocumentDownload: {
            /**
             * Download Url
             * @description Presigned download URL. Honours the file's original content type.
             */
            download_url: string;
            /**
             * Download Url Expires At
             * Format: date-time
             * @description UTC timestamp at which `download_url` stops working.
             */
            download_url_expires_at: string;
            /**
             * File Id
             * @description Stable file identifier. Pass to `/documents/download` to obtain a presigned URL.
             */
            file_id: string;
            /**
             * File Name De
             * @description German display name composed as `"<type label> vom <date>"` (e.g. `Liste der Gesellschafter vom 2026-02-17`).
             */
            file_name_de: string;
            /**
             * File Name En
             * @description English display name composed as `"<type label> from <date>"` (e.g. `Shareholder List from 2026-02-17`).
             */
            file_name_en: string;
            /** @description How current the served file is. `last_checked_at` mirrors the parent `updated_at` (the moment firmendata last fetched this document); `realtime_fetching_status` reports the outcome of the realtime attempt for this request — `download_url` still points at the most recent file on record regardless of that outcome. */
            freshness: components["schemas"]["Freshness"];
            /**
             * Object
             * @description Discriminator. Always `company_document_download`.
             * @default company_document_download
             * @constant
             */
            object: "company_document_download";
            /**
             * Published At
             * @description Publication date as filed in the register. The `register_extract_current` and `register_extract_chronological` document types are not stamped with a publication date by the registry — for those, this falls back to the date portion of `updated_at`.
             */
            published_at?: string | null;
            /**
             * Type
             * @description Kind of register document.
             * @enum {string}
             */
            type: "register_extract_current" | "register_extract_chronological" | "shareholder_list" | "articles_of_association" | "company_registration" | "model_protocol";
            /**
             * Type Label De
             * @description German type name (e.g. `Aktueller Abdruck`, `Chronologischer Abdruck`, `Liste der Gesellschafter`, `Gesellschaftsvertrag` / `Satzung` / `Statut` (the term varies by legal form), `Anmeldung`, `Musterprotokoll`).
             */
            type_label_de: string;
            /**
             * Type Label En
             * @description English type name (e.g. `Current Register Extract`, `Chronological Register Extract`, `Shareholder List`, `Articles of Association`, `Company Registration`, `Model Protocol`).
             */
            type_label_en: string;
            /**
             * Updated At
             * Format: date-time
             * @description When firmendata last fetched this document.
             */
            updated_at: string;
        };
        /**
         * CompanyFinancials
         * @description Full historic + aggregated financial profile for one company.
         *
         *     Returned by `GET /v1/companies/{eu_id}/financials`. Heavier than the
         *     hero detail endpoint — expect multi-year P&L, balance-sheet line
         *     items, employee time series, and parent/subsidiary references.
         */
        CompanyFinancials: {
            /** @description Per-year employee headcount (total / full-time / part-time) sourced from annual Jahresabschluss filings. */
            employee_history: components["schemas"]["EmployeeHistoryBlock"];
            /**
             * Financial Publications
             * @description Filed annual financial statements (Bilanz files) available for this company: Jahresabschluss, Konzernabschluss, or combined. Sorted by `published_at` descending. Attachments reference the filed PDF — downloads are not exposed in v1.
             */
            financial_publications?: components["schemas"]["FinancialPublication"][];
            /** @description How current the financial filings in this response are. `last_checked_at` is the timestamp at which the company's filings were last successfully fetched and parsed; `realtime_fetching_status` reports the outcome of the realtime attempt for this request. */
            freshness: components["schemas"]["Freshness"];
            /** @description Multi-year history: annual top-line metrics plus structured profit-and-loss, assets, and liabilities-and-equity rows from the filings. AI-reconstructed P&L is included for small-company filings that lack a structured statement. */
            history: components["schemas"]["FinancialHistoryBlock"];
            /**
             * Id
             * @description firmendata company identifier.
             */
            id: string;
            /**
             * Object
             * @description Discriminator. Always `company_financials`.
             * @default company_financials
             * @constant
             */
            object: "company_financials";
            /** @description Parent and subsidiary companies. Populated from consolidated financial filings; absent when the company is not part of a reported group. */
            relationships?: components["schemas"]["RelationshipsBlock"] | null;
            /** @description Headline figures for the latest reported fiscal year. `null` when no financial filings are on record. */
            summary?: components["schemas"]["FinancialSummaryBlock"] | null;
        };
        /**
         * CompanyHistory
         * @description Response model for `GET /v1/companies/{eu_id}/history`.
         *
         *     Aggregated view of every change ever recorded against a company in
         *     the Handelsregister, grouped by what was changed (firma, sitz,
         *     kapital, board, etc.). Lists are oldest-first within each group.
         */
        CompanyHistory: {
            /**
             * Board Members
             * @description Board-member appointments and removals (Geschäftsführer, Vorstand, Prokuristen, Aufsichtsrat). Each entry is a single change — to derive the current board, see `current_board`.
             */
            board_members?: components["schemas"]["RegisterHistoryEvent"][];
            /**
             * Company Status
             * @description Current operating status derived from the timeline (`active`, `dissolved`, `insolvent`, …). `null` when no definitive state can be inferred from the history.
             */
            company_status?: string | null;
            /**
             * Current Board
             * @description Snapshot of currently-active board members, derived by walking every appointment / removal in the timeline. Each item carries `name`, `role`, and any qualifying attributes (e.g. `sole_representation`).
             */
            current_board?: {
                [key: string]: unknown;
            }[];
            /** @description First-ever register entry (founding entry). */
            erste_eintragung?: components["schemas"]["RegisterHistoryEvent"] | null;
            /**
             * Eu Id
             * @description firmendata company identifier.
             */
            eu_id: string;
            /**
             * Firma
             * @description Company-name (firma) changes over time.
             */
            firma?: components["schemas"]["RegisterHistoryEvent"][];
            /** @description How current the chronological register history is. `last_checked_at` is the timestamp at which the company's Chronologischer Datenauszug (CD) was last successfully fetched; `realtime_fetching_status` reports the outcome of the realtime attempt for this request. */
            freshness: components["schemas"]["Freshness"];
            /**
             * Gegenstand
             * @description Business-purpose (Unternehmensgegenstand) changes over time.
             */
            gegenstand?: components["schemas"]["RegisterHistoryEvent"][];
            /**
             * Geschaeftsanschrift
             * @description Business-address (Geschäftsanschrift) changes over time.
             */
            geschaeftsanschrift?: components["schemas"]["RegisterHistoryEvent"][];
            /**
             * Kapital
             * @description Share-capital (Stamm-/Grundkapital) changes over time.
             */
            kapital?: components["schemas"]["RegisterHistoryEvent"][];
            /**
             * Kommanditisten
             * @description Limited-partner (Kommanditist) changes — only meaningful for KG-shaped legal forms.
             */
            kommanditisten?: components["schemas"]["RegisterHistoryEvent"][];
            /**
             * Legal Status Events
             * @description Insolvency, Auflösung and Liquidation events, newest first, merged and deduplicated across all three registries. Empty for a company that has never been in insolvency or liquidation.
             */
            legal_status_events?: components["schemas"]["LegalStatusEvent"][];
            /**
             * Object
             * @description Discriminator. Always `company_history`.
             * @default company_history
             * @constant
             */
            object: "company_history";
            /**
             * Rechtsform
             * @description Legal-form (Rechtsform) changes over time.
             */
            rechtsform?: components["schemas"]["RegisterHistoryEvent"][];
            /**
             * Satzung Events
             * @description Articles-of-association amendment entries — covers any change to the company's constitutional document, called Gesellschaftsvertrag / Satzung / Statut in German depending on legal form. The field name `satzung_events` uses the most common German short form.
             */
            satzung_events?: components["schemas"]["RegisterHistoryEvent"][];
            /**
             * Sitz
             * @description Registered seat (Sitz) changes over time.
             */
            sitz?: components["schemas"]["RegisterHistoryEvent"][];
            /**
             * Zweigniederlassungen
             * @description Branch-office (Zweigniederlassung) openings/closings.
             */
            zweigniederlassungen?: components["schemas"]["RegisterHistoryEvent"][];
        };
        /**
         * CompanyListDetailResponse
         * @description Response envelope for `GET /v1/lists/{list_id}`.
         */
        CompanyListDetailResponse: {
            /**
             * Data
             * @description One page of the list's companies. Same row shape as `/companies/search`, so both surfaces deserialize into one type.
             */
            data?: components["schemas"]["SearchHit"][];
            /**
             * Filters Echo
             * @description The effective filter values applied to this query, echoed back for client convenience.
             */
            filters_echo?: {
                [key: string]: unknown;
            };
            /**
             * List Id
             * @description Identifier of the list.
             */
            list_id: number;
            /**
             * Name
             * @description List name as set in the app.
             */
            name: string;
            /**
             * Object
             * @description Discriminator. Always `list`.
             * @default list
             * @constant
             */
            object: "list";
            /** @description Pagination metadata for this page. */
            pagination?: components["schemas"]["PaginationInfo"];
        };
        /**
         * CompanyListSummary
         * @description One of the caller's company lists, without its companies.
         */
        CompanyListSummary: {
            /**
             * Alerts Enabled
             * @description `true` when change alerts are switched on for this list.
             * @default false
             */
            alerts_enabled: boolean;
            /**
             * Company Count
             * @description How many companies are on the list.
             * @default 0
             */
            company_count: number;
            /**
             * Created At
             * Format: date-time
             * @description When the list was created.
             */
            created_at: string;
            /**
             * Is Shared
             * @description `true` when a public share link is currently active.
             * @default false
             */
            is_shared: boolean;
            /**
             * List Id
             * @description Identifier to pass to `GET /v1/lists/{list_id}`.
             */
            list_id: number;
            /**
             * Name
             * @description List name as set in the app.
             */
            name: string;
            /**
             * Object
             * @description Discriminator. Always `company_list`.
             * @default company_list
             * @constant
             */
            object: "company_list";
            /**
             * Updated At
             * Format: date-time
             * @description When the list was last changed.
             */
            updated_at: string;
        };
        /**
         * CompanyListsResponse
         * @description Response envelope for `GET /v1/lists`.
         */
        CompanyListsResponse: {
            /**
             * Data
             * @description The caller's lists, newest first.
             */
            data?: components["schemas"]["CompanyListSummary"][];
            /**
             * Filters Echo
             * @description The effective filter values applied to this query, echoed back for client convenience.
             */
            filters_echo?: {
                [key: string]: unknown;
            };
            /**
             * Object
             * @description Discriminator. Always `list`.
             * @default list
             * @constant
             */
            object: "list";
            /** @description Pagination metadata for this page. */
            pagination?: components["schemas"]["PaginationInfo"];
        };
        /**
         * CompanySize
         * @description Statutory size class under § 267 HGB, read from the filing's own form.
         *
         *     A class is assigned only where the company claimed a size-dependent
         *     relief, which is an assertion of being at most that size. Publishing more
         *     than required is permitted at every size and asserts nothing, so a company
         *     filing a full profit-and-loss statement carries no class — for those the
         *     reported revenue is the answer. The values are therefore not exhaustive,
         *     and selecting both is a real filter rather than a no-op.
         * @enum {string}
         */
        CompanySize: "klein" | "mittelgross";
        /**
         * ContactBlock
         * @description Contact/web-presence block. Populated only when `expand=contact`.
         */
        ContactBlock: {
            /**
             * Impressum Url
             * @description URL of the company's Impressum page.
             */
            impressum_url?: string | null;
            /**
             * Object
             * @description Discriminator. Always `company_contact`.
             * @default company_contact
             * @constant
             */
            object: "company_contact";
            /**
             * Social Profiles
             * @description Discovered social-media profiles.
             */
            social_profiles?: components["schemas"]["SocialProfile"][];
            /**
             * Website
             * @description Primary web domain.
             */
            website?: string | null;
            /**
             * Websites
             * @description All known websites of the company, primary first.
             */
            websites?: components["schemas"]["WebsiteEntry"][];
        };
        /**
         * EmployeeHistoryBlock
         * @description Multi-year employee history. Populated only when `expand=employee_history`.
         */
        EmployeeHistoryBlock: {
            /**
             * Object
             * @description Discriminator. Always `employee_history`.
             * @default employee_history
             * @constant
             */
            object: "employee_history";
            /**
             * Series
             * @description Headcount per year, sorted ascending by year.
             */
            series?: components["schemas"]["EmployeeYear"][];
        };
        /**
         * EmployeeYear
         * @description Employee headcount for one year.
         */
        EmployeeYear: {
            /**
             * Full Time
             * @description Full-time employee count, when reported.
             */
            full_time?: number | null;
            /**
             * Object
             * @description Discriminator. Always `employee_year`.
             * @default employee_year
             * @constant
             */
            object: "employee_year";
            /**
             * Part Time
             * @description Part-time employee count, when reported.
             */
            part_time?: number | null;
            /**
             * Total
             * @description Total employee count for the year.
             */
            total?: number | null;
            /**
             * Year
             * @description Reporting year.
             */
            year: number;
        };
        /**
         * FinancialHistoryBlock
         * @description Multi-year financial history. Populated only when `expand=financial_history`.
         */
        FinancialHistoryBlock: {
            /**
             * Assets
             * @description Filed assets-side rows.
             */
            assets?: components["schemas"]["FinancialRow"][];
            /**
             * Liabilities And Equity
             * @description Filed liabilities-and-equity rows.
             */
            liabilities_and_equity?: components["schemas"]["FinancialRow"][];
            /**
             * Metrics
             * @description Annual top-line metrics, one entry per fiscal year.
             */
            metrics?: components["schemas"]["FinancialMetric"][];
            /**
             * Object
             * @description Discriminator. Always `financial_history`.
             * @default financial_history
             * @constant
             */
            object: "financial_history";
            /**
             * Profit And Loss
             * @description Filed profit-and-loss statement rows.
             */
            profit_and_loss?: components["schemas"]["FinancialRow"][];
        };
        /**
         * FinancialMetric
         * @description Top-line annual financial metrics for one fiscal year.
         */
        FinancialMetric: {
            /** @description Balance-sheet total. */
            balance_sheet_total?: components["schemas"]["Money"] | null;
            /**
             * Ceiling Basis
             * @description Which second criterion forces `revenue_ceiling` — `ceiling_balance_sheet` or `ceiling_employees`. Null whenever `revenue_ceiling` is null.
             * @example ceiling_balance_sheet
             */
            ceiling_basis?: string | null;
            /** @description Rohergebnis (§ 276 HGB) — the collapsed top line a small or medium-sized company may publish *instead of* revenue and cost of materials. Where this is set and `revenue` is null, this is the only top-line figure on file. It is not revenue and must not be compared with it: it is already net of input costs. */
            gross_profit?: components["schemas"]["Money"] | null;
            /** @description Total liabilities and equity. */
            liabilities_and_equity_total?: components["schemas"]["Money"] | null;
            /**
             * Object
             * @description Discriminator. Always `financial_metric`.
             * @default financial_metric
             * @constant
             */
            object: "financial_metric";
            /** @description Annual profit for this year. */
            profit?: components["schemas"]["Money"] | null;
            /** @description Annual revenue (Umsatzerlöse) for this year. Null when the company did not publish it — see `size_class`, which says what the omission itself discloses. */
            revenue?: components["schemas"]["Money"] | null;
            /**
             * @description Highest revenue this company can have had in this fiscal year without losing the size class it filed under — a hard upper bound, not an estimate.
             *
             *     Usually null, and that is the normal case rather than missing data: § 267 asks whether a company exceeds **at least two of three** criteria, so a size class alone permits any revenue. The bound only exists where a second criterion (balance-sheet total or employees) is already over its limit, which forces revenue under its own. Where it is null, no revenue bound follows from the class and none should be inferred.
             * @example {
             *       "amount": 12000000,
             *       "currency": "EUR"
             *     }
             */
            revenue_ceiling?: components["schemas"]["Money"] | null;
            /**
             * Size Basis
             * @description Why `size_class` was assigned, as a stable code:
             *
             *     - `no_guv` — no profit-and-loss statement published (§ 326 Abs. 1)
             *     - `rohergebnis` — P&L collapsed to a Rohergebnis (§ 276)
             *     - `declared` — the filing states its own class
             *     - `ceiling_balance_sheet` / `ceiling_employees` — a second § 267 criterion is over its limit, which tightened the class
             * @example no_guv
             */
            size_basis?: string | null;
            /**
             * Size Class
             * @description Statutory size class for this fiscal year — `kleinst`, `klein`, `mittelgross` or `gross` (§ 267 / § 267a HGB).
             *
             *     Derived from the reliefs the filing claims, so it is an exact statement about the company's own filing, not an estimate. **Null never means large.** It means the filing asserts no size — typically because a full profit-and-loss statement was published, in which case `revenue` carries the answer directly.
             * @example klein
             */
            size_class?: string | null;
            /**
             * Year
             * @description Fiscal year.
             */
            year: number;
        };
        /**
         * FinancialPublication
         * @description One filed financial statement (Jahresabschluss / Konzernabschluss).
         */
        FinancialPublication: {
            /** @description Reference to the PDF filing, when available. */
            attachment?: components["schemas"]["BilanzAttachment"] | null;
            /**
             * Fiscal Year
             * @description Fiscal year the statement covers.
             */
            fiscal_year?: number | null;
            /**
             * Id
             * @description Stable identifier for this filing.
             */
            id: string;
            /**
             * Object
             * @description Discriminator. Always `financial_publication`.
             * @default financial_publication
             * @constant
             */
            object: "financial_publication";
            /**
             * Published At
             * Format: date
             * @description Date the filing was made public.
             */
            published_at: string;
            /**
             * Title
             * @description Filing title as published in the Unternehmensregister (German as filed).
             */
            title: string;
            /**
             * Type
             * @description Kind of financial statement filed.
             * @enum {string}
             */
            type: "financial_statement_annual" | "financial_statement_consolidated" | "financial_statement_combined";
        };
        /**
         * FinancialRow
         * @description One row from a structured financial statement (P&L or balance sheet).
         */
        FinancialRow: {
            /**
             * Current Value
             * @description Value for the current fiscal year, in EUR.
             */
            current_value?: number | null;
            /**
             * Object
             * @description Discriminator. Always `financial_row`.
             * @default financial_row
             * @constant
             */
            object: "financial_row";
            /**
             * Previous Value
             * @description Value for the prior comparison year, in EUR.
             */
            previous_value?: number | null;
            /**
             * Report Type
             * @description Report type (e.g. `Jahresabschluss`, `Konzernabschluss`).
             */
            report_type: string;
            /**
             * Section
             * @description Top-level section heading.
             */
            section?: string | null;
            /**
             * Subsection
             * @description Second-level heading.
             */
            subsection?: string | null;
            /**
             * Subsubsection
             * @description Third-level heading.
             */
            subsubsection?: string | null;
            /**
             * Subsubsubsection
             * @description Fourth-level heading.
             */
            subsubsubsection?: string | null;
            /**
             * Title
             * @description Line-item title as filed.
             */
            title: string;
            /**
             * Year
             * @description Fiscal year of the statement this row belongs to.
             */
            year: number;
        };
        /**
         * FinancialSummaryBlock
         * @description Headline financial figures for the latest reported fiscal year.
         */
        FinancialSummaryBlock: {
            /**
             * Latest Employee Count
             * @description Latest reported employee count.
             */
            latest_employee_count?: number | null;
            /**
             * Latest Fiscal Year
             * @description Fiscal year of the latest reported figures.
             */
            latest_fiscal_year?: number | null;
            /** @description Latest reported total assets. */
            latest_total_assets?: components["schemas"]["Money"] | null;
            /** @description Latest reported total liabilities and equity. */
            latest_total_liabilities_and_equity?: components["schemas"]["Money"] | null;
            /**
             * Object
             * @description Discriminator. Always `financial_summary`.
             * @default financial_summary
             * @constant
             */
            object: "financial_summary";
        };
        /**
         * FounderProfileBlock
         * @description Founder, family-involvement and succession assessment.
         *
         *     Every field is **derived** from the register's board history rather
         *     than read off a filing, so each verdict ships with the evidence that
         *     produced it. The three verdicts are deliberately nullable and `null`
         *     never means "no": it means the register does not support an answer.
         */
        FounderProfileBlock: {
            /**
             * As Of
             * @description Date of the register snapshot this rests on. A director who resigned after this date is not yet reflected.
             */
            as_of?: string | null;
            /**
             * Evidence Source
             * @description Which register sources backed this assessment: `cd` (chronological extract), `si` (structured snapshot) or `cd+si`.
             */
            evidence_source?: string | null;
            /**
             * Family Confidence
             * @description `confirmed` rests on two identified people; `likely` on the name alone.
             */
            family_confidence?: ("confirmed" | "likely") | null;
            /**
             * Family Managed
             * @description Whether two or more members of one family hold an ownership or governing role. `null` when the register shows only one person and the company name carries no family signal — there is then nothing to compare against, which is not the same as unrelated.
             */
            family_managed?: boolean | null;
            /**
             * Family Signals
             * @description Which signals fired: `shared_surname`, `eponymous` (a person's surname is in the company name), `lexical` (`Gebr.`, `& Söhne`), `multigenerational` (relatives a generation apart), `shared_address`.
             * @example [
             *       "shared_surname",
             *       "eponymous"
             *     ]
             */
            family_signals?: string[];
            /**
             * Family Surname
             * @description Normalised surname the family signal rests on.
             */
            family_surname?: string | null;
            /**
             * Founder Count
             * @description How many natural persons were identified at first registration.
             * @default 0
             */
            founder_count: number;
            /**
             * Founder Led
             * @description Whether someone holding a role at first registration is still in office. `null` when the chronological extract is missing, or begins with a transfer from another register court — its first entry then records the move, not the founding.
             */
            founder_led?: boolean | null;
            /**
             * Founder Led Confidence
             * @description `likely` when the company was renamed shortly after registration, the signature of a shelf company (Vorratsgesellschaft) whose first officer is the provider's nominee rather than a founder.
             */
            founder_led_confidence?: ("confirmed" | "likely") | null;
            /**
             * Founder Led Since
             * @description Start of the longest-serving founder's tenure.
             */
            founder_led_since?: string | null;
            /**
             * Founders
             * @description The identified founders.
             */
            founders?: components["schemas"]["FounderRecordItem"][];
            /**
             * Object
             * @description Discriminator. Always `founder_profile`.
             * @default founder_profile
             * @constant
             */
            object: "founder_profile";
            /**
             * Oldest Founder Birth Year
             * @description Birth year of the oldest founder still serving.
             */
            oldest_founder_birth_year?: number | null;
            /**
             * Succession Risk
             * @description Whether the oldest serving founder has reached 65. `null` when the company is not founder-led, or no founder birth date is on record.
             */
            succession_risk?: boolean | null;
        };
        /**
         * FounderRecordItem
         * @description One person identified as holding a role at first registration.
         */
        FounderRecordItem: {
            /**
             * Birth Year
             * @description Year of birth, when the register records a birth date.
             */
            birth_year?: number | null;
            /**
             * Entry Date
             * @description Date of the first register entry, `DD.MM.YYYY`.
             */
            entry_date?: string | null;
            /**
             * First Name
             * @description Given name as filed.
             */
            first_name?: string | null;
            /**
             * Last Name
             * @description Family name as filed.
             */
            last_name?: string | null;
            /**
             * Object
             * @description Discriminator. Always `founder`.
             * @default founder
             * @constant
             */
            object: "founder";
            /**
             * Role
             * @description Role held at first registration (e.g. `Geschäftsführer`, `Kommanditist`).
             * @example Geschäftsführer
             */
            role?: string | null;
            /**
             * Share Percent
             * @description Capital share held at founding, in percent. Only limited partners (Kommanditisten) record this in the register; for every other legal form the founding cap table is not public register data and this is `null`.
             */
            share_percent?: number | null;
            /**
             * Still Serving
             * @description Whether this person is still in office.
             * @default false
             */
            still_serving: boolean;
        };
        /**
         * Freshness
         * @description How current the data in this response is.
         *
         *     See the `Data Freshness` section of the API overview for the
         *     three modes (cached read, realtime fetch, subscriptions) and how
         *     `last_checked_at` is computed per endpoint.
         */
        Freshness: {
            /**
             * Last Checked At
             * @description Timestamp (UTC) of the last successful refresh of the underlying data slice from the German registries. Compare against your own staleness tolerance to decide whether to re-issue the call with `fetch_realtime=true`. `null` when the slice has never been successfully fetched for this company.
             * @example 2026-04-30T12:14:08Z
             */
            last_checked_at?: string | null;
            /**
             * Object
             * @description Discriminator. Always `freshness`.
             * @default freshness
             * @constant
             */
            object: "freshness";
            /**
             * Realtime Fetching Status
             * @description Outcome of the realtime fetch attempt for this request (`fetch_realtime=true`). `realtime_fetching_disabled` when the parameter was not set; `success` when fresh data was merged into the response; otherwise the response falls back to the cached read and this field reports why.
             * @enum {string}
             */
            realtime_fetching_status: "success" | "platform_unavailable" | "documents_temporarily_unavailable" | "file_unavailable" | "company_unavailable" | "realtime_fetching_disabled";
        };
        /**
         * IndustryBlock
         * @description Industry block: ranked WZ 2025 classifications.
         */
        IndustryBlock: {
            /**
             * Object
             * @description Discriminator. Always `company_industry`.
             * @default company_industry
             * @constant
             */
            object: "company_industry";
            /**
             * Primary Wz 2025 Code
             * @description Primary (highest-confidence) WZ 2025 code. Dotted-hierarchy format, e.g. `62.01.9`.
             */
            primary_wz_2025_code?: string | null;
            /**
             * Wz 2025 Codes
             * @description Ranked WZ 2025 candidates with confidence scores.
             */
            wz_2025_codes?: components["schemas"]["Wz2025Score"][];
        };
        /**
         * IndustrySlug
         * @enum {string}
         */
        IndustrySlug: "automotive" | "chemicals" | "construction" | "consulting" | "education" | "energy" | "financial_services" | "food" | "healthcare" | "holdings" | "hospitality" | "insurance" | "legal" | "logistics" | "manufacturing" | "media" | "mining" | "public_sector" | "technology" | "telecom" | "trade";
        /**
         * InsolvencyBlock
         * @description Insolvency / Auflösung / Liquidation snapshot.
         *
         *     Present whenever any legal-status event is on record — a voluntary
         *     liquidation reports ``has_insolvency=false`` with a ``current_status``
         *     of ``in_liquidation``.
         */
        InsolvencyBlock: {
            /**
             * Case Number
             * @description Insolvency case number (Aktenzeichen).
             */
            case_number?: string | null;
            /**
             * Court Name
             * @description Name of the insolvency court, when proceedings are open.
             */
            court_name?: string | null;
            /**
             * Current Status
             * @description Current stage of the proceeding. `preliminary` — vorläufige Insolvenzverwaltung / Sicherungsmaßnahmen. `in_insolvency` — Verfahren eröffnet. `rejected` — Abweisung mangels Masse. `terminated` — Verfahren aufgehoben. `in_liquidation` / `dissolved` — no insolvency proceeding, the company was dissolved under corporate law. `active` — no proceeding.
             * @default active
             * @enum {string}
             */
            current_status: "active" | "preliminary" | "in_insolvency" | "rejected" | "terminated" | "in_liquidation" | "dissolved";
            /**
             * Has Insolvency
             * @description `true` when an insolvency *proceeding* is on record. A company dissolved by shareholder resolution reports `false` here while still reporting `current_status` of `in_liquidation`.
             * @default false
             */
            has_insolvency: boolean;
            /**
             * Has Liquidator
             * @description `true` when a Liquidator or Abwickler is in office.
             * @default false
             */
            has_liquidator: boolean;
            /**
             * Is Dissolved
             * @description `true` when the company is dissolved (aufgelöst).
             * @default false
             */
            is_dissolved: boolean;
            /**
             * Most Recent Event At
             * @description Date of the most recent insolvency event on record.
             */
            most_recent_event_at?: string | null;
            /**
             * Object
             * @description Discriminator. Always `insolvency_status`.
             * @default insolvency_status
             * @constant
             */
            object: "insolvency_status";
            /**
             * Self Administration
             * @description `true` when the proceeding is in self-administration (Eigenverwaltung).
             * @default false
             */
            self_administration: boolean;
        };
        /**
         * LegalStatus
         * @description Company lifecycle buckets for the search filter.
         *
         *     Disjoint and exhaustive — every company in the index is in exactly one,
         *     and `deleted` outranks the rest (a deleted company that was also in
         *     liquidation counts as deleted, matching the company detail page).
         * @enum {string}
         */
        LegalStatus: "active" | "in_liquidation" | "insolvent" | "dissolved" | "deleted";
        /**
         * LegalStatusEvent
         * @description One insolvency / Auflösung / Liquidation event.
         *
         *     Deduplicated across the three sources, so a single event carries every
         *     registry that recorded it in `sources` rather than appearing three times.
         */
        LegalStatusEvent: {
            /**
             * Case Number
             * @description Insolvency case number (Aktenzeichen), when the entry states one.
             */
            case_number?: string | null;
            /**
             * Category
             * @description Event type, in the shared German vocabulary: `Sicherungsmaßnahmen`, `Eröffnungen`, `Abweisungen mangels Masse`, `Aufhebung Sicherungsmaßnahmen`, `Aufhebung des Verfahrens`, `Auflösung durch Gesellschafterbeschluss`, `Auflösung mit Fortführung`, `Auflösung`.
             * @example Auflösung durch Gesellschafterbeschluss
             */
            category: string;
            /**
             * Court Name
             * @description Court named in the entry.
             */
            court_name?: string | null;
            /**
             * Decision Date
             * @description Date of the court order or shareholder resolution (Beschluss).
             */
            decision_date?: string | null;
            /**
             * Dissolved
             * @description `true` when this event dissolves the company.
             * @default false
             */
            dissolved: boolean;
            /**
             * Effective Date
             * @description When a dissolution takes effect, if later than the decision.
             */
            effective_date?: string | null;
            /**
             * Entry Number
             * @description Running register entry number (laufende Nummer).
             */
            entry_number?: number | null;
            /**
             * Event Date
             * @description Register entry date (SI / CD) or portal publication date. `null` when the source records no date.
             */
            event_date?: string | null;
            /**
             * Ex Officio
             * @description `true` when entered von Amts wegen.
             * @default false
             */
            ex_officio: boolean;
            /**
             * Legal Basis
             * @description Statutory citations found in the entry text.
             * @example [
             *       "§ 60 Abs. 1 GmbHG"
             *     ]
             */
            legal_basis?: string[];
            /**
             * Object
             * @description Discriminator. Always `legal_status_event`.
             * @default legal_status_event
             * @constant
             */
            object: "legal_status_event";
            /**
             * Self Administration
             * @description `true` for Eigenverwaltung (§ 270 InsO).
             * @default false
             */
            self_administration: boolean;
            /**
             * Sources
             * @description Registries that recorded this event. `si` — Strukturierte Inhalte, `cd` — Chronologisches Datenblatt, `portal` — Insolvenzbekanntmachungen.
             */
            sources?: ("si" | "cd" | "portal")[];
        };
        /**
         * LeiGraphBlock
         * @description LEI-based ownership graph. Populated only when `expand=lei_graph`.
         */
        LeiGraphBlock: {
            /**
             * Direct Children
             * @description Direct children in the LEI graph.
             */
            direct_children?: components["schemas"]["LeiRelative"][];
            /** @description Direct parent in the LEI graph, when reported. */
            direct_parent?: components["schemas"]["LeiRelative"] | null;
            /**
             * Lei Code
             * @description The company's own LEI code, when assigned.
             */
            lei_code?: string | null;
            /**
             * Object
             * @description Discriminator. Always `lei_graph`.
             * @default lei_graph
             * @constant
             */
            object: "lei_graph";
        };
        /**
         * LeiRelative
         * @description A node in the LEI-derived ownership graph.
         */
        LeiRelative: {
            /**
             * City
             * @description Headquarters city from the LEI record.
             */
            city?: string | null;
            /**
             * Country Code
             * @description ISO 3166-1 alpha-2 country code.
             */
            country_code?: string | null;
            /**
             * Display Name
             * @description Best human-readable name to use in UIs.
             */
            display_name?: string | null;
            /**
             * Eu Id
             * @description firmendata company identifier when the entity is known to firmendata.
             */
            eu_id?: string | null;
            /**
             * Legal Name
             * @description Full legal name from the LEI record.
             */
            legal_name?: string | null;
            /**
             * Lei Code
             * @description Legal Entity Identifier (ISO 17442).
             */
            lei_code: string;
            /**
             * Object
             * @description Discriminator. Always `lei_relative`.
             * @default lei_relative
             * @constant
             */
            object: "lei_relative";
            /**
             * Register Court
             * @description Name of the registering court (Registergericht).
             */
            register_court?: string | null;
            /**
             * Register Number
             * @description Register entry number (digits only). Combine with `register_type` for a full reference.
             * @example 12345
             */
            register_number?: number | null;
            /**
             * Register Type
             * @description Register type code (e.g. `HRB`, `HRA`, `GnR`, `PR`, `VR`).
             */
            register_type?: string | null;
            /**
             * Relationship
             * @description Relationship type (e.g. `direct_parent`, `direct_child`).
             */
            relationship: string;
        };
        /**
         * LocalizedText
         * @description A piece of text rendered in German and/or English.
         */
        LocalizedText: {
            /**
             * De
             * @description German rendering of the text, if available.
             */
            de?: string | null;
            /**
             * En
             * @description English rendering of the text, if available.
             */
            en?: string | null;
            /**
             * Object
             * @description Discriminator. Always `localized_text`.
             * @default localized_text
             * @constant
             */
            object: "localized_text";
            /**
             * Translation Source
             * @description Provenance of the rendering. `original`: the value as filed; `human`: human translation; `machine`: machine translation; `passthrough`: identical to the source language.
             * @default original
             * @enum {string}
             */
            translation_source: "original" | "human" | "machine" | "passthrough";
        };
        /**
         * Member
         * @description A management or representative member of the company as filed in the register.
         */
        Member: {
            /**
             * Object
             * @description Discriminator. Always `member`.
             * @default member
             * @constant
             */
            object: "member";
            /** @description Organisation, when applicable. */
            organization?: components["schemas"]["OrganizationRef"] | null;
            /** @description Natural person, when applicable. */
            person?: components["schemas"]["Person"] | null;
            /**
             * Role
             * @description Role label as filed.
             */
            role?: string | null;
            /**
             * Role Code
             * @description firmendata-internal role code.
             */
            role_code?: string | null;
        };
        /**
         * Membership
         * @description External-registry memberships used as a company filter.
         *
         *     Each value renders to an ``EXISTS`` predicate against a side table
         *     that carries ``eu_id`` back to ``company.eu_id``. The repository
         *     OR-merges the predicates when multiple values are supplied.
         * @enum {string}
         */
        Membership: "Steuerberaterkammer" | "Wirtschaftsprueferkammer" | "bbh" | "BaFin" | "LEI";
        /**
         * Money
         * @description A monetary amount in the given currency.
         */
        Money: {
            /**
             * Amount
             * @description Numeric amount in `currency` units. `null` when not reported.
             * @example 1250000
             */
            amount?: number | null;
            /**
             * Currency
             * @description ISO 4217 currency code. Defaults to EUR.
             * @default EUR
             */
            currency: string;
            /**
             * Object
             * @description Discriminator. Always `money`.
             * @default money
             * @constant
             */
            object: "money";
        };
        /**
         * NotificationCadence
         * @description How often you wish to receive notifications.
         *
         *     A notification is only delivered when there is an actual update
         *     since the previous successful delivery — no update means no
         *     notification. For ``one_time`` subscriptions, this is the retry
         *     spacing while we wait for the data to become available.
         *
         *     ``immediately`` delivers an update as soon as it is detected —
         *     service-level objective: within 1 hour of publication.
         *     ``daily`` / ``weekly`` / ``monthly`` batch the check at the named
         *     cadence and notify only on actual changes.
         * @enum {string}
         */
        NotificationCadence: "immediately" | "daily" | "weekly" | "monthly";
        /**
         * NotificationLanguage
         * @description Display language for human-targeted notifications (currently
         *     just email subject + body). Webhook payloads carry both languages
         *     in a bilingual ``summary`` block regardless of this setting.
         * @enum {string}
         */
        NotificationLanguage: "en" | "de";
        /**
         * NotificationType
         * @description Transport for the notification.
         *
         *     * ``webhook``: HTTP POST a signed JSON payload to ``webhook_url``.
         *     * ``email``:   SMTP (Zoho) deliver a bilingual email to ``recipient_email``.
         *
         *     A subscription with ``notification_type = None`` is async (poll-only):
         *     the dispatcher performs no outbound delivery, records the change in
         *     ``subscription_events``, and the customer retrieves the rendered
         *     payload via the events API using the subscription/job id returned at
         *     create time.
         * @enum {string}
         */
        NotificationType: "webhook" | "email";
        /**
         * OrganizationRef
         * @description Reference to another organisation (parent, subsidiary, shareholder org).
         */
        OrganizationRef: {
            /**
             * Country Code
             * @description ISO 3166-1 alpha-2 country code of the organisation, when known.
             */
            country_code?: string | null;
            /**
             * Display Name
             * @description Best human-readable name to use in UIs.
             */
            display_name: string;
            /**
             * Eu Id
             * @description firmendata company identifier when the organisation is known to firmendata, otherwise `null`.
             */
            eu_id?: string | null;
            /**
             * Legal Name
             * @description Full legal name as filed.
             */
            legal_name: string;
            /**
             * Lei Code
             * @description Legal Entity Identifier (ISO 17442), when assigned.
             */
            lei_code?: string | null;
            /**
             * Object
             * @description Discriminator. Always `organization_ref`.
             * @default organization_ref
             * @constant
             */
            object: "organization_ref";
            /**
             * Register Court
             * @description Name of the registering court (Registergericht).
             */
            register_court?: string | null;
            /**
             * Register Number
             * @description Register entry number (digits only). Combine with `register_type` for a full reference.
             * @example 12345
             */
            register_number?: number | null;
            /**
             * Register Type
             * @description Register type code (e.g. `HRB`, `HRA`, `GnR`, `PR`, `VR`).
             */
            register_type?: string | null;
            /**
             * Registered Seat
             * @description Registered seat city, when known.
             */
            registered_seat?: string | null;
        };
        /**
         * PaginationInfo
         * @description Cursor-pagination metadata included in every list response.
         */
        PaginationInfo: {
            /**
             * Has More
             * @description `true` when more pages are available after this one.
             * @default false
             */
            has_more: boolean;
            /**
             * Next Cursor
             * @description Opaque cursor to fetch the next page. `null` on the last page.
             */
            next_cursor?: string | null;
            /**
             * Object
             * @description Discriminator. Always `pagination`.
             * @default pagination
             * @constant
             */
            object: "pagination";
            /**
             * Previous Cursor
             * @description Opaque cursor to fetch the previous page. `null` on the first page.
             */
            previous_cursor?: string | null;
            /**
             * Total Approx
             * @description Approximate total result count. Capped at 10,000 for `/companies/search`; exact for other list endpoints.
             */
            total_approx?: number | null;
            /**
             * Total Is Capped
             * @description `true` when `total_approx` was clamped to the cap. Narrow your query for an exact count.
             * @default false
             */
            total_is_capped: boolean;
        };
        /**
         * Person
         * @description A natural person referenced by the company (board member, shareholder, etc.).
         */
        Person: {
            /**
             * Age
             * @description Approximate current age in years, derived from `date_of_birth`.
             */
            age?: number | null;
            /**
             * City
             * @description City of residence as filed.
             */
            city?: string | null;
            /**
             * Date Of Birth
             * @description Date of birth, when reported by the register. May be a full date or a year-only date (`YYYY-01-01`) when only the year was filed.
             */
            date_of_birth?: string | null;
            /**
             * First Name
             * @description Given name.
             */
            first_name?: string | null;
            /**
             * Full Name
             * @description Pre-composed display name.
             */
            full_name: string;
            /**
             * Last Name
             * @description Family name.
             */
            last_name?: string | null;
            /**
             * Middle Name
             * @description Middle name(s), if filed.
             */
            middle_name?: string | null;
            /**
             * Object
             * @description Discriminator. Always `person`.
             * @default person
             * @constant
             */
            object: "person";
            /**
             * Title
             * @description Honorific or academic title (e.g. `Dr.`).
             */
            title?: string | null;
        };
        /**
         * PotentialBeneficialOwner
         * @description An unresolved organisation whose effective share at the root crosses
         *     the §3 GwG 25 % threshold. The natural person(s) controlling this
         *     entity could be ultimate beneficial owners — we just couldn't follow
         *     the chain past it (foreign country, no German Gesellschafterliste,
         *     or depth cap).
         */
        PotentialBeneficialOwner: {
            /**
             * Chain Positions
             * @description Number of distinct root-edges through which this organisation accumulated its effective share. >1 means the same name+city+country appeared as the apex of multiple controlled branches.
             */
            chain_positions: number;
            /**
             * Effective Share Percent
             * @description Effective share at the root summed across every chain position where this organisation appears. Computed under the same all-or-nothing rule as `BeneficialOwner.effective_share_percent` (root-edge share for each >25 % control chain reaching the entity, plus direct holdings).
             */
            effective_share_percent: number;
            /**
             * Object
             * @description Discriminator. Always `potential_beneficial_owner`.
             * @default potential_beneficial_owner
             * @constant
             */
            object: "potential_beneficial_owner";
            /** @description The unresolved organisation node — same shape as `UboNode.organization`, including any partial registry metadata we have. */
            organization: components["schemas"]["OrganizationRef"];
            /**
             * Unresolved Reason
             * @description Why we couldn't continue the chain past this entity — matches the `UboNode.unresolved_reason` of the same node in `graph`.
             * @enum {string}
             */
            unresolved_reason: "foreign_org" | "unlisted_org" | "cap_table_not_filed" | "max_depth_reached";
        };
        /**
         * Problem
         * @description RFC 7807 problem details object.
         */
        Problem: {
            /** @description Human-readable explanation specific to this occurrence. */
            detail?: string | null;
            /** @description Field-level validation failures (only on 422 responses). */
            errors?: {
                /** @description Machine-readable error code. */
                code?: string;
                /** @description Dotted path to the offending field. */
                path?: string;
            }[] | null;
            /** @description URI reference identifying the specific occurrence (typically the request path). */
            instance?: string | null;
            /** @description Echoes the `X-Request-Id` request header, or a server-generated UUID. Use when contacting support. */
            request_id?: string;
            /**
             * @description HTTP status code.
             * @example 422
             */
            status: number;
            /**
             * @description Short, human-readable summary of the problem.
             * @example Request validation failed.
             */
            title: string;
            /**
             * Format: uri
             * @description URI identifying the problem type.
             * @example https://api.firmendata.com/problems/validation-error
             */
            type: string;
        };
        /**
         * Rechtsform
         * @enum {string}
         */
        Rechtsform: "GmbH" | "KG" | "e.K." | "eGbR" | "UG" | "GmbH & Co. KG" | "OHG" | "PartG" | "e.V." | "AG" | "eG" | "ausländische Rechtsform (HRB)" | "gGmbH" | "UG & Co. KG" | "ausländische Rechtsform" | "SE" | "sonstige juristische Person" | "EWIV" | "KGaA" | "Ltd. & Co. KG" | "sonstige juristische Person (HRA)" | "GmbH & Co. OHG" | "VVaG" | "SE & Co. KG" | "ausländische Rechtsform (HRA)" | "AG & Co. KG" | "ausländische Rechtsform (PR)" | "SCE" | "ausländische Rechtsform (GnR)" | "Stiftung & Co. KG" | "eG & Co. KG";
        /**
         * RegisterCourt
         * @enum {string}
         */
        RegisterCourt: "Aachen" | "Ahaus" | "Amberg" | "Ansbach" | "Arnsberg" | "Aschaffenburg" | "Augsburg" | "Aurich" | "Bad Hersfeld" | "Bad Homburg v.d.H." | "Bad Kreuznach" | "Bad Oeynhausen" | "Bamberg" | "Bayreuth" | "Berlin (Charlottenburg)" | "Bielefeld" | "Bocholt" | "Bochum" | "Bonn" | "Borken" | "Braunschweig" | "Bremen" | "Bremerhaven" | "Chemnitz" | "Coburg" | "Coesfeld" | "Cottbus" | "Darmstadt" | "Deggendorf" | "Dortmund" | "Dresden" | "Duisburg" | "Dülmen" | "Düren" | "Düsseldorf" | "Eschwege" | "Essen" | "Flensburg" | "Frankfurt am Main" | "Frankfurt/Oder" | "Freiburg" | "Friedberg" | "Fritzlar" | "Fulda" | "Fürth" | "Gelsenkirchen" | "Gießen" | "Gronau" | "Göttingen" | "Gütersloh" | "Hagen" | "Hamburg" | "Hamm" | "Hanau" | "Hannover" | "Hildesheim" | "Hof" | "Homburg" | "Ingolstadt" | "Iserlohn" | "Jena" | "Kaiserslautern" | "Kassel" | "Kempten (Allgäu)" | "Kerpen" | "Kiel" | "Kleve" | "Koblenz" | "Korbach" | "Krefeld" | "Köln" | "Königstein" | "Landau" | "Landshut" | "Langenfeld" | "Lebach" | "Leipzig" | "Lemgo" | "Limburg" | "Ludwigshafen a.Rhein (Ludwigshafen)" | "Lübeck" | "Lüdinghausen" | "Lüneburg" | "Mainz" | "Mannheim" | "Marburg" | "Memmingen" | "Merzig" | "Montabaur" | "Mönchengladbach" | "München" | "Münster" | "Neubrandenburg" | "Neunkirchen" | "Neuruppin" | "Neuss" | "Nürnberg" | "Offenbach am Main" | "Oldenburg (Oldenburg)" | "Osnabrück" | "Ottweiler" | "Paderborn" | "Passau" | "Pinneberg" | "Potsdam" | "Recklinghausen" | "Regensburg" | "Rostock" | "Saarbrücken" | "Saarlouis" | "Schleiden" | "Schweinfurt" | "Schwerin" | "Siegburg" | "Siegen" | "St. Ingbert (St Ingbert)" | "St. Wendel (St Wendel)" | "Stadthagen" | "Steinfurt" | "Stendal" | "Stralsund" | "Straubing" | "Stuttgart" | "Tostedt" | "Traunstein" | "Ulm" | "Völklingen" | "Walsrode" | "Weiden i. d. OPf." | "Wetzlar" | "Wiesbaden" | "Wittlich" | "Wuppertal" | "Würzburg" | "Zweibrücken";
        /**
         * RegisterHistoryEvent
         * @description One entry in the chronological register timeline.
         *
         *     ``entry_date`` is the German DD.MM.YYYY format the registry
         *     publishes; consumers that want a parsed date should split on
         *     ``"."``. ``data`` is the raw aggregated record — its keys depend
         *     on which timeline this event belongs to (firma vs. kapital vs.
         *     board, etc.).
         */
        RegisterHistoryEvent: {
            /**
             * Data
             * @description Aggregated record body. Keys vary by category — e.g. firma events carry `firma`/`prev_firma`; kapital events carry `kapital`/`waehrung`; board events carry `name`/`role`/`action`. See the API guide for the full per-category schema.
             */
            data?: {
                [key: string]: unknown;
            };
            /**
             * Entry Date
             * @description Date of the register entry in DD.MM.YYYY (the format the Handelsregister publishes). `null` for entries the parser could not date-stamp.
             */
            entry_date?: string | null;
            /**
             * Entry Number
             * @description Numbered position of this entry in the chronological register (laufende Nummer der Eintragung).
             */
            entry_number?: number | null;
            /**
             * Object
             * @description Discriminator. Always `register_history_event`.
             * @default register_history_event
             * @constant
             */
            object: "register_history_event";
        };
        /**
         * RegisterType
         * @enum {string}
         */
        RegisterType: "HRA" | "HRB" | "GnR" | "PR" | "VR" | "GsR" | "CH-HR";
        /**
         * RelatedCompany
         * @description Reference to a related company (parent or subsidiary).
         */
        RelatedCompany: {
            /**
             * Bilanz Type
             * @description Type of the financial statement the relationship was sourced from.
             */
            bilanz_type?: string | null;
            /**
             * Display Name
             * @description Best human-readable name to use in UIs.
             */
            display_name: string;
            /**
             * Eu Id
             * @description firmendata company identifier when available, otherwise `null`.
             */
            eu_id?: string | null;
            /**
             * Legal Name
             * @description Full legal name as filed.
             */
            legal_name: string;
            /**
             * Object
             * @description Discriminator. Always `related_company`.
             * @default related_company
             * @constant
             */
            object: "related_company";
            /**
             * Register Court
             * @description Name of the registering court (Registergericht).
             */
            register_court?: string | null;
            /**
             * Register Number
             * @description Register entry number (digits only). Combine with `register_type` for a full reference.
             * @example 12345
             */
            register_number?: number | null;
            /**
             * Register Type
             * @description Register type code (e.g. `HRB`, `HRA`, `GnR`, `PR`, `VR`).
             */
            register_type?: string | null;
            /**
             * Registered Seat
             * @description Registered seat city.
             */
            registered_seat?: string | null;
            /**
             * Reporting Year
             * @description Fiscal year the relationship was reported in.
             */
            reporting_year?: number | null;
        };
        /**
         * RelationshipsBlock
         * @description Parent + subsidiaries graph. Populated only when `expand=relationships`.
         */
        RelationshipsBlock: {
            /**
             * Object
             * @description Discriminator. Always `company_relationships`.
             * @default company_relationships
             * @constant
             */
            object: "company_relationships";
            /** @description Reported parent company, when known. */
            parent_company?: components["schemas"]["RelatedCompany"] | null;
            /**
             * Subsidiaries
             * @description Reported subsidiaries.
             */
            subsidiaries?: components["schemas"]["RelatedCompany"][];
        };
        /**
         * SearchHit
         * @description One search result.
         */
        SearchHit: {
            /** @description Registered seat address. */
            address?: components["schemas"]["Address"] | null;
            /**
             * Cpv Award Codes
             * @description CPV (Common Procurement Vocabulary) codes from public-procurement awards.
             */
            cpv_award_codes?: string[];
            /**
             * Display Name
             * @description Best human-readable name to use in UIs.
             */
            display_name: string;
            /**
             * Employee Count
             * @description Most recently reported employee count.
             */
            employee_count?: number | null;
            /**
             * Employee Count Year
             * @description Fiscal year (`YYYY`) that `employee_count` is for.
             */
            employee_count_year?: number | null;
            /**
             * Eu Id
             * @description firmendata company identifier.
             */
            eu_id: string;
            /**
             * Financial Year
             * @description The company's latest filing year (`YYYY`) — the most recent year for which *any* financial statement is on file. This is a filing-activity indicator, **not** the year of the figures above; for those use `revenue_year`, `profit_year` and `total_assets_year`. Typically 1–3 years behind the current calendar year due to German Handelsregister publication lag. Null when no financials have been filed.
             */
            financial_year?: number | null;
            /**
             * Founded At
             * @description Founding date, when reported.
             */
            founded_at?: string | null;
            /**
             * Legal Name
             * @description Full legal name as filed.
             */
            legal_name: string;
            /**
             * Legal Status
             * @description Lifecycle status, resolved from the merged Handelsregister (SI + CD) and Insolvenzbekanntmachungen timelines. Same vocabulary as `CompanyDetail.status` and the `legal_status` search filter.
             * @default active
             * @enum {string}
             */
            legal_status: "active" | "in_liquidation" | "insolvent" | "dissolved" | "deleted";
            /**
             * Object
             * @description Discriminator. Always `search_hit`.
             * @default search_hit
             * @constant
             */
            object: "search_hit";
            /**
             * Profit
             * @description Most recently reported annual profit in EUR (Jahresüberschuss / Bilanzgewinn). The fiscal year it is for is `profit_year`, which is **not** guaranteed to equal `revenue_year`.
             */
            profit?: number | null;
            /**
             * Profit Year
             * @description Fiscal year (`YYYY`) that `profit` is for.
             */
            profit_year?: number | null;
            /**
             * Register Court
             * @description Name of the registering court (Registergericht).
             */
            register_court?: string | null;
            /**
             * Register Number
             * @description Register entry number (digits only). Combine with `register_type` for a full reference.
             * @example 12345
             */
            register_number?: number | null;
            /**
             * Register Type
             * @description Register type code (e.g. `HRB`, `HRA`, `GnR`, `PR`, `VR`).
             */
            register_type?: string | null;
            /**
             * Revenue
             * @description Most recently reported annual revenue in EUR (Umsatzerlöse). The fiscal year it is for is `revenue_year`. Null when the company has never disclosed one — which is the common case, not an edge case: small and medium-sized filers publish abridged accounts with no profit-and-loss statement. Use `total_assets` for a size measure with far broader coverage.
             */
            revenue?: number | null;
            /**
             * Revenue Year
             * @description Fiscal year (`YYYY`) that `revenue` is for. May differ from `profit_year`, `total_assets_year` and `financial_year` — each figure is resolved to the most recent filing that actually carries it, so do not assume two of them share a year when computing ratios.
             */
            revenue_year?: number | null;
            /**
             * Total Assets
             * @description Balance-sheet total (Bilanzsumme) in EUR, from the most recent filing that reports one. Every filing company publishes a balance sheet — abridged accounts omit the profit-and-loss statement but not this — which makes it the most widely available measure of company size.
             */
            total_assets?: number | null;
            /**
             * Total Assets Year
             * @description Fiscal year (`YYYY`) that `total_assets` is for.
             */
            total_assets_year?: number | null;
            /**
             * Website
             * @description Primary web domain associated with the company.
             */
            website?: string | null;
        };
        /**
         * SearchResponse
         * @description Response envelope for `GET /v1/companies/search`.
         */
        SearchResponse: {
            /**
             * Data
             * @description Search hits for this page.
             */
            data?: components["schemas"]["SearchHit"][];
            /**
             * Execution Time Ms
             * @description Server-side query execution time in milliseconds.
             */
            execution_time_ms?: number | null;
            /**
             * Filters Echo
             * @description The effective filter values applied to this query, echoed back for client convenience.
             */
            filters_echo?: {
                [key: string]: unknown;
            };
            /** @description Outcome of the `fetch_realtime` register discovery for this request. `last_checked_at` is always `null` here — the hits span many companies, so no single slice timestamp applies. */
            freshness?: components["schemas"]["Freshness"];
            /**
             * Object
             * @description Discriminator. Always `list`.
             * @default list
             * @constant
             */
            object: "list";
            /** @description Pagination metadata for this page. */
            pagination?: components["schemas"]["PaginationInfo"];
        };
        /**
         * Shareholder
         * @description One shareholder entry in the cap table.
         */
        Shareholder: {
            /**
             * Display Name
             * @description Best human-readable name to use in UIs.
             */
            display_name: string;
            /**
             * Object
             * @description Discriminator. Always `shareholder`.
             * @default shareholder
             * @constant
             */
            object: "shareholder";
            /** @description Organisation details (when `party_type` is `organization`). */
            organization?: components["schemas"]["OrganizationRef"] | null;
            /**
             * Party Type
             * @description Type of shareholder. Determines whether `person` or `organization` is populated.
             * @enum {string}
             */
            party_type: "natural_person" | "organization";
            /** @description Natural-person details (when `party_type` is `natural_person`). */
            person?: components["schemas"]["Person"] | null;
            /**
             * Share Percent
             * @description Aggregated share percentage in [0, 100].
             */
            share_percent?: number | null;
            /** @description Nominal value of the shares held. */
            shares_nominal?: components["schemas"]["Money"] | null;
            /**
             * Total Shares
             * @description Total number of shares held.
             */
            total_shares?: number | null;
        };
        /**
         * ShareholdersCoverage
         * @description Coverage signal for the shareholders endpoint.
         */
        ShareholdersCoverage: {
            /**
             * Object
             * @description Discriminator. Always `shareholders_coverage`.
             * @default shareholders_coverage
             * @constant
             */
            object: "shareholders_coverage";
            /**
             * Status
             * @description Coverage status. `available`: cap table parsed and returned; `not_applicable`: legal form does not file a Gesellschafterliste; `not_filed`: filing expected but not yet received; `token_limit_reached`: filing too large to process.
             * @default not_filed
             * @enum {string}
             */
            status: "available" | "not_applicable" | "not_filed" | "token_limit_reached";
        };
        /**
         * ShareholdersReport
         * @description Response model for `GET /v1/companies/{eu_id}/shareholders`.
         */
        ShareholdersReport: {
            /** @description Latest cap-table snapshot. `null` unless `coverage.status == "available"`. */
            as_of_snapshot?: components["schemas"]["ShareholdersSnapshot"] | null;
            /** @description Coverage status for this company. */
            coverage: components["schemas"]["ShareholdersCoverage"];
            /**
             * Eu Id
             * @description firmendata company identifier.
             */
            eu_id: string;
            /** @description How current the cap-table snapshot in this response is. `last_checked_at` is the timestamp at which the underlying Liste der Gesellschafter (or Musterprotokoll fallback) was last successfully ingested for this company; `null` when `coverage.status != "available"`. `realtime_fetching_status` reports the outcome of the realtime attempt for this request. */
            freshness: components["schemas"]["Freshness"];
            /**
             * Object
             * @description Discriminator. Always `shareholders_report`.
             * @default shareholders_report
             * @constant
             */
            object: "shareholders_report";
        };
        /**
         * ShareholdersSnapshot
         * @description A point-in-time cap table snapshot.
         */
        ShareholdersSnapshot: {
            /**
             * Object
             * @description Discriminator. Always `shareholders_snapshot`.
             * @default shareholders_snapshot
             * @constant
             */
            object: "shareholders_snapshot";
            /**
             * Shareholders
             * @description Shareholders in this snapshot.
             */
            shareholders?: components["schemas"]["Shareholder"][];
        };
        /**
         * SocialProfile
         * @description A link to one of the company's social-media profiles.
         */
        SocialProfile: {
            /**
             * Object
             * @description Discriminator. Always `social_profile`.
             * @default social_profile
             * @constant
             */
            object: "social_profile";
            /**
             * Platform
             * @description Platform identifier (e.g. `linkedin`, `instagram`).
             */
            platform: string;
            /**
             * Url
             * @description Canonical profile URL.
             */
            url: string;
        };
        /**
         * Sort
         * @description How to order search results.
         *
         *     Companies missing the value always sort last, whichever direction is
         *     chosen. `total_assets` ranks the most companies of the size measures:
         *     every filing company publishes a balance sheet, where only a minority
         *     publish a full profit-and-loss statement.
         * @enum {string}
         */
        Sort: "name" | "revenue" | "profit" | "total_assets" | "employee_count";
        /**
         * SortDirection
         * @enum {string}
         */
        SortDirection: "asc" | "desc";
        /**
         * Subscription
         * @description A webhook or email subscription, with the most recent delivery
         *     attempt embedded.
         *
         *     The signing secret is **never** returned here; only
         *     :class:`SubscriptionCreated` carries it, and only once.
         */
        Subscription: {
            /** @description How often you wish to receive notifications. A delivery is only made when the underlying data has actually changed since `latest_succeeded_publish_date` — no change means no notification. `immediately` delivers an update as soon as it is detected, with a service-level objective of within 1 hour of publication. `daily` / `weekly` / `monthly` batch the check at the named cadence. `null` only for legacy subscriptions whose stored cadence predates this enum. */
            cadence: components["schemas"]["NotificationCadence"] | null;
            /**
             * Cadence Seconds
             * @description Canonical numeric form of `cadence`.
             */
            cadence_seconds: number;
            /**
             * Created At
             * Format: date-time
             * @description UTC timestamp of subscription creation.
             */
            created_at: string;
            /**
             * Eu Id
             * @description Company the subscription is scoped to.
             */
            eu_id: string;
            /**
             * Id
             * @description UUID of the subscription.
             */
            id: string;
            /**
             * @description Display language for human-targeted notifications. Webhook payloads carry both languages in `summary` regardless of this setting; only email subject + body are rendered in this language.
             * @default en
             */
            language: components["schemas"]["NotificationLanguage"];
            /**
             * Last Attempted At
             * @description UTC timestamp of the most recent delivery attempt.
             */
            last_attempted_at?: string | null;
            /** @description Detail of the most recent delivery attempt. `null` until the first attempt has been made. */
            last_event?: components["schemas"]["SubscriptionEventStatus"] | null;
            /**
             * Latest Succeeded Publish Date
             * @description Publish date of the document last successfully delivered. The next webhook is sent only when a strictly newer publish date appears.
             */
            latest_succeeded_publish_date?: string | null;
            /** @description `recurring` keeps delivering forever at the configured `cadence`. `one_time` keeps polling at the configured `cadence` until the first successful delivery, then stops. The row stays around as a record — `DELETE` it when you're done. */
            mode: components["schemas"]["SubscriptionMode"];
            /** @description Delivery transport — `webhook`, `email`, or `null` for async (poll-only) subscriptions, where no outbound delivery is performed and the customer consumes change events via the events API. */
            notification_type: components["schemas"]["NotificationType"] | null;
            /**
             * Object
             * @description Discriminator. Always `subscription`.
             * @default subscription
             * @constant
             */
            object: "subscription";
            /**
             * Recipient Email
             * @description Email recipient. Populated only when `notification_type=email`.
             */
            recipient_email?: string | null;
            /**
             * Subscription End Date
             * @description Optional terminal date. After this calendar day the subscription stops checking for updates; the configuration stays in place until you `DELETE` it.
             */
            subscription_end_date?: string | null;
            /** @description Slice of company data this subscription tracks. See the `SubscriptionType` schema for the per-value coverage table — in particular: `shareholders` auto-falls-back to Musterprotokoll for simplified-procedure GmbHs, while `articles_of_association` does not (pick `model_protocol` instead for those companies). */
            subscription_type: components["schemas"]["SubscriptionType"];
            /**
             * Updated At
             * Format: date-time
             * @description UTC timestamp of last subscription update.
             */
            updated_at: string;
            /**
             * Webhook Url
             * @description Webhook receiver URL. Populated only when `notification_type=webhook`. https only.
             */
            webhook_url?: string | null;
        };
        /** SubscriptionCreateRequest */
        SubscriptionCreateRequest: {
            /**
             * @description How often you wish to receive notifications. **Default: `daily`.** A notification is dispatched only when there is an actual change since the previous successful delivery — no update means no notification. `immediately` delivers an update as soon as it is detected, with a service-level objective of within 1 hour of publication. `daily` / `weekly` / `monthly` batch the check at the named cadence. For `one_time` subscriptions this is the retry spacing while we wait for the data to become available.
             * @default daily
             */
            cadence: components["schemas"]["NotificationCadence"];
            /**
             * Eu Id
             * @description firmendata company identifier to subscribe against.
             * @example DEF1103R.HRB279792B
             */
            eu_id: string;
            /**
             * @description Display language for human-targeted notifications. **Default: `en`.** Currently affects only email subject + body — webhook payloads carry both languages in the bilingual `summary` block regardless of this setting.
             * @default en
             */
            language: components["schemas"]["NotificationLanguage"];
            /**
             * @description Delivery behaviour. **Default: `recurring`.**
             *
             *     - `recurring` — the **first** delivery is a snapshot of the current state. After that, you only get a notification when there is an actual update.
             *     - `one_time` — keep polling at the configured `cadence` *until* the first successful delivery, then stop. Useful when:
             *       - the document you're waiting for is not yet available on Handelsregister (e.g. a Gesellschafterliste not yet filed, a Bilanz not yet published in Bundesanzeiger),
             *       - or Handelsregister is currently failing to serve the file due to a technical issue on their end (transient 5xx, anti-bot blocks) and you want firmendata to keep retrying on your behalf without you having to call the API on a schedule.
             *       The subscription stays in place after the first successful delivery as a record — `DELETE` it when you're done.
             * @default recurring
             */
            mode: components["schemas"]["SubscriptionMode"];
            /**
             * @description Delivery transport. **Default: `null`** — async (poll-only).
             *
             *     - `null` — no outbound delivery; the dispatcher records each change as a `subscription_event` and the customer retrieves the rendered payload by polling `GET /v1/subscriptions/{id}/events` using the subscription/job id this request returns. Must omit both `webhook_url` and `recipient_email`.
             *     - `webhook` — POST a signed JSON body to `webhook_url`. Requires `webhook_url`; rejects `recipient_email`.
             *     - `email` — SMTP a bilingual notification to `recipient_email`. Requires `recipient_email`; rejects `webhook_url`.
             */
            notification_type?: components["schemas"]["NotificationType"] | null;
            /**
             * Recipient Email
             * @description Email address to deliver notifications to. Required when `notification_type=email`; must be omitted for `notification_type=webhook`.
             * @example alerts@example.com
             */
            recipient_email?: string | null;
            /**
             * Subscription End Date
             * @description Optional terminal date. After this calendar day the subscription stops checking for updates. **Default: `null`** — open-ended subscription.
             */
            subscription_end_date?: string | null;
            /** @description Which slice of company data to track. See the `SubscriptionType` schema for the per-value coverage table — `shareholders` works for every GmbH/UG (it auto-falls-back to Musterprotokoll when no Liste is filed), while `doc_articles_of_association` does not — pick `doc_model_protocol` for the founding deed of simplified-procedure companies. */
            subscription_type: components["schemas"]["SubscriptionType"];
            /**
             * Webhook Url
             * @description https-only URL that will receive webhook calls. Required when `notification_type=webhook`; must be omitted for `notification_type=email`.
             * @example https://example.com/firmendata/webhook
             */
            webhook_url?: string | null;
        };
        /**
         * SubscriptionCreated
         * @description Response shape for ``POST /v1/subscriptions``. Identical to
         *     :class:`Subscription`. For webhook subscriptions it additionally
         *     carries the plaintext signing secret — only available here, never
         *     returned by any other endpoint. For email subscriptions
         *     ``webhook_secret`` is ``null`` (signatures don't apply to SMTP).
         */
        SubscriptionCreated: {
            /** @description How often you wish to receive notifications. A delivery is only made when the underlying data has actually changed since `latest_succeeded_publish_date` — no change means no notification. `immediately` delivers an update as soon as it is detected, with a service-level objective of within 1 hour of publication. `daily` / `weekly` / `monthly` batch the check at the named cadence. `null` only for legacy subscriptions whose stored cadence predates this enum. */
            cadence: components["schemas"]["NotificationCadence"] | null;
            /**
             * Cadence Seconds
             * @description Canonical numeric form of `cadence`.
             */
            cadence_seconds: number;
            /**
             * Created At
             * Format: date-time
             * @description UTC timestamp of subscription creation.
             */
            created_at: string;
            /**
             * Eu Id
             * @description Company the subscription is scoped to.
             */
            eu_id: string;
            /**
             * Id
             * @description UUID of the subscription.
             */
            id: string;
            /**
             * @description Display language for human-targeted notifications. Webhook payloads carry both languages in `summary` regardless of this setting; only email subject + body are rendered in this language.
             * @default en
             */
            language: components["schemas"]["NotificationLanguage"];
            /**
             * Last Attempted At
             * @description UTC timestamp of the most recent delivery attempt.
             */
            last_attempted_at?: string | null;
            /** @description Detail of the most recent delivery attempt. `null` until the first attempt has been made. */
            last_event?: components["schemas"]["SubscriptionEventStatus"] | null;
            /**
             * Latest Succeeded Publish Date
             * @description Publish date of the document last successfully delivered. The next webhook is sent only when a strictly newer publish date appears.
             */
            latest_succeeded_publish_date?: string | null;
            /** @description `recurring` keeps delivering forever at the configured `cadence`. `one_time` keeps polling at the configured `cadence` until the first successful delivery, then stops. The row stays around as a record — `DELETE` it when you're done. */
            mode: components["schemas"]["SubscriptionMode"];
            /** @description Delivery transport — `webhook`, `email`, or `null` for async (poll-only) subscriptions, where no outbound delivery is performed and the customer consumes change events via the events API. */
            notification_type: components["schemas"]["NotificationType"] | null;
            /**
             * Object
             * @description Discriminator. Always `subscription_created`.
             * @default subscription_created
             * @constant
             */
            object: "subscription_created";
            /**
             * Recipient Email
             * @description Email recipient. Populated only when `notification_type=email`.
             */
            recipient_email?: string | null;
            /**
             * Subscription End Date
             * @description Optional terminal date. After this calendar day the subscription stops checking for updates; the configuration stays in place until you `DELETE` it.
             */
            subscription_end_date?: string | null;
            /** @description Slice of company data this subscription tracks. See the `SubscriptionType` schema for the per-value coverage table — in particular: `shareholders` auto-falls-back to Musterprotokoll for simplified-procedure GmbHs, while `articles_of_association` does not (pick `model_protocol` instead for those companies). */
            subscription_type: components["schemas"]["SubscriptionType"];
            /**
             * Updated At
             * Format: date-time
             * @description UTC timestamp of last subscription update.
             */
            updated_at: string;
            /**
             * Webhook Secret
             * @description HMAC-SHA256 signing secret. Populated only for `notification_type=webhook` subscriptions. Store it somewhere safe — firmendata cannot re-display it. Use it to verify the `X-Firmendata-Signature` header on every incoming webhook (the signed string is `f"{X-Firmendata-Timestamp}.{raw_request_body}"`). Always `null` for `notification_type=email`.
             */
            webhook_secret?: string | null;
            /**
             * Webhook Url
             * @description Webhook receiver URL. Populated only when `notification_type=webhook`. https only.
             */
            webhook_url?: string | null;
        };
        /**
         * SubscriptionEvent
         * @description Full event record with the exact bytes firmendata sent on the
         *     wire. Returned by the historical event endpoints — heavier than
         *     :class:`SubscriptionEventStatus`, which is the trimmed status block
         *     embedded in subscription detail responses.
         *
         *     For email rows and historical rows that predate the request-body
         *     column migration, every ``request_*`` field is ``null``.
         */
        SubscriptionEvent: {
            /**
             * Change Detected
             * @description True iff the latest check produced a publish date strictly newer than the previously delivered one.
             */
            change_detected: boolean;
            /**
             * Delivery Attempted
             * @description False when no notification was sent (e.g. silent no-change, unsupported, ended).
             */
            delivery_attempted: boolean;
            /**
             * Delivery Attempts
             * @description Number of in-run delivery attempts for this event. Webhook: 0–3 (retried on 5xx / transport errors). Email: 0–1 (SMTP failures wait for the next cadence).
             */
            delivery_attempts: number;
            /**
             * Delivery Duration Ms
             * @description Wall-clock time of the last delivery attempt in milliseconds.
             */
            delivery_duration_ms?: number | null;
            /**
             * Delivery Error
             * @description Short error string from a failing delivery attempt.
             */
            delivery_error?: string | null;
            /**
             * Delivery Http Status
             * @description Webhook only: HTTP status of the last in-run attempt, or `null` on transport-level failure. Always `null` for email and for async (poll-only) events.
             */
            delivery_http_status?: number | null;
            /**
             * Delivery Response Excerpt
             * @description First ~512 chars of your endpoint's response body, captured for debugging. Webhook only.
             */
            delivery_response_excerpt?: string | null;
            /**
             * Event Id
             * @description UUID echoed in the `X-Firmendata-Delivery-Id` header of the outgoing webhook. Use it to correlate this status with the request your endpoint received.
             */
            event_id: string;
            /**
             * Finished At
             * @description UTC timestamp the attempt finished, or `null` while still in progress.
             */
            finished_at?: string | null;
            /** @description Transport this event was delivered over — `webhook`, `email`, or `null` for async (poll-only) subscriptions. Inherited from the parent subscription, which is immutable, so every event for a given subscription carries the same value. For email and async events the `request_url` / `request_signature` / `request_timestamp` / `delivery_http_status` fields are always `null`. For async events `request_body` is populated (the customer consumes the change payload through the events API); for email it is null. */
            notification_type: components["schemas"]["NotificationType"] | null;
            /**
             * Object
             * @description Discriminator. Always `subscription_event`.
             * @default subscription_event
             * @constant
             */
            object: "subscription_event";
            /**
             * Outcome
             * @description What happened on this attempt. One of: `change_delivered`, `no_change_silent`, `webhook_delivery_failed`, `scrape_failed_company_not_found`, `scrape_failed_file_not_found`, `scrape_failed_documents_temporarily_unavailable`, `scrape_failed_platform_error`, `scrape_failed_parser_error`, `scrape_failed_unknown`, `skipped_unsupported`, `skipped_ended`.
             */
            outcome: string;
            /**
             * Publish Date Previous
             * @description The previous `latest_succeeded_publish_date`, for diffing.
             */
            publish_date_previous?: string | null;
            /**
             * Publish Date Seen
             * @description Publish date observed in the latest check (max register entry date or file publication date).
             */
            publish_date_seen?: string | null;
            /**
             * Request Body
             * @description The exact JSON body firmendata POSTed to your endpoint. Null for email events and for events recorded before the request body was persisted (rows older than 2026-05-02). Re-verify the signature with: `hmac_sha256(secret, f'{request_timestamp}.{json.dumps(request_body, separators=(",",":"))}')`.
             */
            request_body?: {
                [key: string]: unknown;
            } | null;
            /**
             * Request Signature
             * @description The `X-Firmendata-Signature` header firmendata sent. Format: `sha256=<hex>`.
             */
            request_signature?: string | null;
            /**
             * Request Timestamp
             * @description The `X-Firmendata-Timestamp` header (unix-seconds) firmendata sent.
             */
            request_timestamp?: string | null;
            /**
             * Request Url
             * @description The webhook URL firmendata used for this attempt. May differ from the subscription's current `webhook_url` if the URL was changed via PATCH after the event.
             */
            request_url?: string | null;
            /**
             * Scrape Error Message
             * @description Compact, customer-safe description of a failed data check. One sentence — names the company id and the data slice in plain language; never echoes internal class names. Examples: `"No shareholder list available for DEF1103R.HRB279792B on Handelsregister."`, `"Handelsregister was temporarily unreachable while checking the articles of association for DEF1103R.HRB279792B."`. The dispatcher re-attempts every failed scrape on the configured `cadence`, so most messages don't repeat that — the only exception is `scrape_failed_company_not_found`, which is effectively terminal until the company reappears in the registry. `null` for successful or non-failure events.
             */
            scrape_error_message?: string | null;
            /**
             * Started At
             * Format: date-time
             * @description UTC timestamp the attempt began.
             */
            started_at: string;
        };
        /** SubscriptionEventList */
        SubscriptionEventList: {
            /**
             * Items
             * @description Events for the subscription, newest first.
             */
            items?: components["schemas"]["SubscriptionEvent"][];
            /**
             * Object
             * @description Discriminator. Always `list`.
             * @default list
             * @constant
             */
            object: "list";
        };
        /**
         * SubscriptionEventResendResponse
         * @description Response shape for ``POST /v1/subscriptions/events/{event_id}/resend``.
         */
        SubscriptionEventResendResponse: {
            /**
             * Duration Ms
             * @description Wall-clock time of the resend in milliseconds.
             */
            duration_ms: number;
            /**
             * Error
             * @description Short error string when the resend did not succeed.
             */
            error?: string | null;
            /** @description The new event row recording this resend. Has its own fresh `event_id`. The receiver sees that same id in the body and in the `X-Firmendata-Delivery-Id` header. */
            event: components["schemas"]["SubscriptionEvent"];
            /**
             * Http Status
             * @description HTTP status of the last in-run attempt, or `null` on transport failure.
             */
            http_status?: number | null;
            /**
             * Object
             * @description Discriminator. Always `subscription_event_resend`.
             * @default subscription_event_resend
             * @constant
             */
            object: "subscription_event_resend";
            /**
             * Succeeded
             * @description True iff the receiver returned a 2xx response.
             */
            succeeded: boolean;
        };
        /**
         * SubscriptionEventStatus
         * @description Status block describing the most recent delivery attempt for a
         *     subscription. Embedded in subscription detail responses.
         */
        SubscriptionEventStatus: {
            /**
             * Change Detected
             * @description True iff the latest check produced a publish date strictly newer than the previously delivered one.
             */
            change_detected: boolean;
            /**
             * Delivery Attempted
             * @description False when no notification was sent (e.g. silent no-change, unsupported, ended).
             */
            delivery_attempted: boolean;
            /**
             * Delivery Attempts
             * @description Number of in-run delivery attempts for this event. Webhook: 0–3 (retried on 5xx / transport errors). Email: 0–1 (SMTP failures wait for the next cadence).
             */
            delivery_attempts: number;
            /**
             * Delivery Duration Ms
             * @description Wall-clock time of the last delivery attempt in milliseconds.
             */
            delivery_duration_ms?: number | null;
            /**
             * Delivery Error
             * @description Short error string from a failing delivery attempt.
             */
            delivery_error?: string | null;
            /**
             * Delivery Http Status
             * @description Webhook only: HTTP status of the last in-run attempt, or `null` on transport-level failure. Always `null` for email and for async (poll-only) events.
             */
            delivery_http_status?: number | null;
            /**
             * Event Id
             * @description UUID echoed in the `X-Firmendata-Delivery-Id` header of the outgoing webhook. Use it to correlate this status with the request your endpoint received.
             */
            event_id: string;
            /**
             * Finished At
             * @description UTC timestamp the attempt finished, or `null` while still in progress.
             */
            finished_at?: string | null;
            /**
             * Object
             * @description Discriminator. Always `subscription_event_status`.
             * @default subscription_event_status
             * @constant
             */
            object: "subscription_event_status";
            /**
             * Outcome
             * @description What happened on this attempt. One of: `change_delivered`, `no_change_silent`, `webhook_delivery_failed`, `scrape_failed_company_not_found`, `scrape_failed_file_not_found`, `scrape_failed_documents_temporarily_unavailable`, `scrape_failed_platform_error`, `scrape_failed_parser_error`, `scrape_failed_unknown`, `skipped_unsupported`, `skipped_ended`.
             */
            outcome: string;
            /**
             * Publish Date Previous
             * @description The previous `latest_succeeded_publish_date`, for diffing.
             */
            publish_date_previous?: string | null;
            /**
             * Publish Date Seen
             * @description Publish date observed in the latest check (max register entry date or file publication date).
             */
            publish_date_seen?: string | null;
            /**
             * Scrape Error Message
             * @description Compact, customer-safe description of a failed data check. One sentence — names the company id and the data slice in plain language; never echoes internal class names. Examples: `"No shareholder list available for DEF1103R.HRB279792B on Handelsregister."`, `"Handelsregister was temporarily unreachable while checking the articles of association for DEF1103R.HRB279792B."`. The dispatcher re-attempts every failed scrape on the configured `cadence`, so most messages don't repeat that — the only exception is `scrape_failed_company_not_found`, which is effectively terminal until the company reappears in the registry. `null` for successful or non-failure events.
             */
            scrape_error_message?: string | null;
            /**
             * Started At
             * Format: date-time
             * @description UTC timestamp the attempt began.
             */
            started_at: string;
        };
        /** SubscriptionList */
        SubscriptionList: {
            /**
             * Items
             * @description Subscriptions owned by the calling user.
             */
            items?: components["schemas"]["Subscription"][];
            /**
             * Object
             * @description Discriminator. Always `list`.
             * @default list
             * @constant
             */
            object: "list";
        };
        /**
         * SubscriptionMode
         * @description Recurring delivery vs single-shot poll-until-success (one_time).
         * @enum {string}
         */
        SubscriptionMode: "recurring" | "one_time";
        /**
         * SubscriptionTestRequest
         * @description Inputs for ``POST /v1/subscriptions/test``.
         *
         *     Same discriminator rules as the create payload: webhook
         *     subscriptions need ``webhook_url``, email subscriptions need
         *     ``recipient_email``. Sending the wrong combination returns ``422``.
         */
        SubscriptionTestRequest: {
            /**
             * @description How to deliver the test notification. Defaults to `success` (single signed POST). Use `retry_delivery` to exercise your idempotent dedupe, `bad_signature` to exercise your HMAC-rejection path, or `stale_timestamp` to exercise your replay-window rejection. Webhook-only — ignored when `notification_type=email`.
             * @default success
             */
            delivery_scenario: components["schemas"]["WebhookTestDeliveryScenario"];
            /**
             * Eu Id
             * @description Company identifier embedded in the test payload. Does not need to be known to firmendata — only used to compose the example links.
             * @example DEF1103R.HRB279792B
             */
            eu_id: string;
            /**
             * @description Display language for the test email. Ignored for `notification_type=webhook`.
             * @default en
             */
            language: components["schemas"]["NotificationLanguage"];
            /** @description Transport to test — `webhook` or `email`. Async (poll-only) subscriptions have no outbound transport to exercise; pick `webhook` or `email` here. */
            notification_type: components["schemas"]["NotificationType"];
            /**
             * Recipient Email
             * @description Email address that will receive the test message. Required for `notification_type=email`.
             * @example alerts@example.com
             */
            recipient_email?: string | null;
            /**
             * Scenario
             * @description Kind of notification to send. Today only `change` is supported (the only event firmendata currently delivers). Additional scenarios may be added later.
             * @default change
             * @constant
             */
            scenario: "change";
            /** @description Subscription type to mimic. Determines the bilingual summary, the webhook `event_type` header (e.g. `articles_of_association.updated`), the email subject + body, and the `data.links` block. See the `SubscriptionType` schema for the per-value coverage table. */
            subscription_type: components["schemas"]["SubscriptionType"];
            /**
             * Webhook Url
             * @description https-only URL the test event will be POSTed to. Required for `notification_type=webhook`.
             * @example https://example.com/firmendata/webhook
             */
            webhook_url?: string | null;
        };
        /**
         * SubscriptionTestResponse
         * @description Outputs for ``POST /v1/subscriptions/test``.
         *
         *     Transport-shaped: the webhook fields populate when the request was
         *     a webhook test, the email fields populate when it was an email
         *     test. The common ``receiver_*`` block describes the outcome
         *     regardless of transport.
         */
        SubscriptionTestResponse: {
            /**
             * Attempts
             * @description Per-attempt detail. One element for `success`, `bad_signature`, and `stale_timestamp`; two for `retry_delivery` (sharing the same signature, timestamp, body, and `X-Firmendata-Delivery-Id`). `null` for email tests.
             */
            attempts?: components["schemas"]["WebhookTestAttempt"][] | null;
            /** @description Echo of the `delivery_scenario` requested. `null` for email tests. */
            delivery_scenario?: components["schemas"]["WebhookTestDeliveryScenario"] | null;
            /**
             * Duration Ms
             * @description Wall-clock milliseconds for the entire test — sum across attempts when `delivery_scenario=retry_delivery`.
             */
            duration_ms: number;
            /** @description Transport that was tested. */
            notification_type: components["schemas"]["NotificationType"];
            /**
             * Object
             * @description Discriminator. Always `subscription_test_result`.
             * @default subscription_test_result
             * @constant
             */
            object: "subscription_test_result";
            /**
             * Receiver Error
             * @description Short error string when the test failed.
             */
            receiver_error?: string | null;
            /**
             * Receiver Http Status
             * @description Webhook only: HTTP status the receiver returned. `null` for email tests or transport-level failure.
             */
            receiver_http_status?: number | null;
            /**
             * Receiver Response Excerpt
             * @description Webhook only: first ~512 chars of the receiver's response body.
             */
            receiver_response_excerpt?: string | null;
            /**
             * Receiver Succeeded
             * @description Webhook: the receiver returned a 2xx status. Email: the SMTP server accepted the message.
             */
            receiver_succeeded: boolean;
            /**
             * Recipient Email
             * @description Address the test email was sent to. `null` for webhook tests.
             */
            recipient_email?: string | null;
            /**
             * Sent Body
             * @description Exact JSON body sent to the webhook. `null` for email tests.
             */
            sent_body?: {
                [key: string]: unknown;
            } | null;
            /**
             * Sent Body Html
             * @description Full HTML body of the test email. `null` for webhook tests.
             */
            sent_body_html?: string | null;
            /**
             * Sent Event Id
             * @description Value sent in `X-Firmendata-Delivery-Id`. `null` for email tests.
             */
            sent_event_id?: string | null;
            /**
             * Sent Event Type
             * @description Value sent in `X-Firmendata-Event-Type`. `null` for email tests.
             */
            sent_event_type?: string | null;
            /**
             * Sent Signature Header
             * @description Full `X-Firmendata-Signature` header value. `null` for email tests.
             */
            sent_signature_header?: string | null;
            /**
             * Sent Subject
             * @description Subject line of the test email. `null` for webhook tests.
             */
            sent_subject?: string | null;
            /**
             * Sent Timestamp Header
             * @description Full `X-Firmendata-Timestamp` header value. `null` for email tests.
             */
            sent_timestamp_header?: string | null;
            /**
             * Webhook Secret
             * @description HMAC-SHA256 signing secret used for the webhook test. Always the same documented public constant `firmendata_test_secret_do_not_use_in_production`, so you can hard-code it. `null` for email tests.
             */
            webhook_secret?: string | null;
            /**
             * Webhook Url
             * @description URL the test was POSTed to. `null` for email tests.
             */
            webhook_url?: string | null;
        };
        /**
         * SubscriptionType
         * @description Slice of company data a subscription tracks. Two flavours:
         *
         *     - **Parsed-data** types — `details`, `history`, `shareholders`, `ubo`. The webhook body's `data` block carries a publish-date timestamp and `links` to the v1 endpoints that serve the parsed JSON.
         *     - **Document** types — every `doc_*` value. The webhook body's `data.download.url` is a 7-day presigned URL to the PDF, so the customer can fetch the file directly without a follow-up API call. After expiry, `GET /v1/companies/{eu_id}/documents/download` mints a fresh URL.
         *
         *     **Coverage is not uniform across types.** German company law lets the smallest GmbH / UG (haftungsbeschränkt) replace the Articles of Association (Gesellschaftsvertrag / Satzung / Statut — the name varies by legal form) and the Liste der Gesellschafter with a single **Musterprotokoll** (model protocol) at founding. firmendata handles this asymmetrically by intent:
         *
         *     - `shareholders` **auto-falls-back** to the Musterprotokoll when no Liste der Gesellschafter is filed — pick this for cap-table tracking regardless of legal form.
         *     - `doc_articles_of_association` does **not** fall back — pick `doc_model_protocol` when you need the founding deed of a simplified-procedure company.
         *     - `doc_shareholder_list` does **not** fall back — pick `doc_model_protocol` for Musterprotokoll-only companies, or `shareholders` for parsed cap-table data that works for any GmbH/UG.
         *     - `doc_*` subscriptions only fire for companies that actually filed the matching document; mismatches return `scrape_failed_file_not_found` until the document appears (use `mode=one_time` if you're waiting for a one-off filing).
         *
         *     | Value | What it tracks |
         *     |---|---|
         *     | `details` | Current company snapshot — identity, registered address, current board, share capital, and lifecycle status. The webhook body's `data.links.company_detail` resolves to `GET /v1/companies/{eu_id}` for the full parsed JSON. Available for every Handelsregister-registered company. |
         *     | `history` | Chronological register history — every recorded change to company name (firma), seat (Sitz), capital, board, business purpose, etc. The webhook body's `data.links.company_history` resolves to `GET /v1/companies/{eu_id}/history` for the full parsed timeline. Available for every Handelsregister-registered company. |
         *     | `shareholders` | Cap table — primarily from the **Liste der Gesellschafter** (list of shareholders), filed by GmbH and UG (haftungsbeschränkt). **Auto-fallback to Musterprotokoll:** companies founded via the simplified procedure under §2 Abs. 1a GmbHG do not file a separate Liste — their **Musterprotokoll** (model protocol) names the founders and their share allocations directly. firmendata handles this transparently: when no Liste has been filed, the `shareholders` subscription derives the cap table from the Musterprotokoll instead. So `shareholders` is the right subscription for cap-table updates regardless of which document the company filed. The webhook body links to `GET /v1/companies/{eu_id}/shareholders` for the parsed cap table. |
         *     | `ubo` | Beneficial owners (**wirtschaftlich Berechtigte**) — natural persons who ultimately own or control the company, derived by walking the Gesellschafter graph. Each dispatch refreshes the cap-table document for **every** German GmbH/UG in the ownership chain (root and parents), not just the root, and fires when **any** chain member's Liste der Gesellschafter (or Musterprotokoll fallback) gets a newer publish date. The webhook body carries the full UBO report (graph, `beneficial_owners`, `potential_beneficial_owners`, `coverage` with `reason`) plus a per-node `chain` breakdown (`eu_id`, `depth`, refresh `status`, latest `publish_date`).  `coverage.status="partial"` means one or more unresolved branches carry effective share above 25 % at the root and could hide a UBO — `potential_beneficial_owners` lists those entities (foreign holdings, unlisted parents, depth-capped branches) with their summed effective share so KYC consumers can decide whether to escalate. `coverage.status="available"` with `has_unresolved_branches=true` means unresolved branches exist but are immaterial (combined share below the threshold or isolated by sub-threshold control links).  Transient per-node failures (e.g. `documents_temporarily_unavailable`, `platform_unavailable`) defer the dispatch — `latest_succeeded_publish_date` is not advanced and the next cadence retries; chain members that succeeded today are dedup'd at the registry layer so retries only re-hit the failed nodes' documents. Deterministic per-node outcomes (e.g. `file_unavailable`, `skipped_not_applicable` for non-GmbH chain members) are surfaced in the chain breakdown but do **not** block delivery — the rest of the chain still ships. |
         *     | `doc_articles_of_association` | **Articles of association** document — Germany's constitutional company document, called **Gesellschaftsvertrag** (GmbH, KG, OHG, GbR), **Satzung** (AG, KGaA, eG, Verein), or **Statut** (some specific forms) depending on legal form. All three are filed under the same registry slot.  Webhook body's `data.download.url` is a 7-day presigned URL to the PDF — the customer can fetch the file directly without a follow-up API call. After the URL expires, `GET /v1/companies/{eu_id}/documents/download?file_type=articles_of_association` mints a fresh one.  **Not all companies have one.** Companies founded via the simplified procedure under §2 Abs. 1a GmbHG file a **Musterprotokoll** in its place — subscribe to `doc_model_protocol` for those. |
         *     | `doc_company_registration` | **Initial company registration filing (Anmeldung zur Eintragung)** — the notarial document submitted to register the company. Webhook body carries a 7-day presigned download URL (`data.download.url`). Available for any newly-registered company; fires when a new Anmeldung is filed. |
         *     | `doc_model_protocol` | **Model protocol document (Musterprotokoll)** — the simplified notarial founding deed for small GmbH and UG (haftungsbeschränkt) companies under §2 Abs. 1a GmbHG. Legally substitutes for **both** the Articles of Association and the Liste der Gesellschafter for these companies. The Musterprotokoll itself names the founders, their share allocations, and the basic company terms in a single document.  Webhook body carries a 7-day presigned download URL (`data.download.url`). Only companies founded via the simplified procedure have one; for everyone else this subscription reports `scrape_failed_file_not_found`. For **cap-table changes** — regardless of which document was filed — subscribe to `shareholders` instead (it auto-falls-back to the Musterprotokoll when no Liste has been filed). |
         *     | `doc_shareholder_list` | **Liste der Gesellschafter** document — the standalone shareholder-list filing. Webhook body carries a 7-day presigned download URL (`data.download.url`).  Distinct from the parsed `shareholders` subscription: this tracks the **document itself** with no Musterprotokoll fallback, so a Musterprotokoll-only GmbH (founded under §2 Abs. 1a GmbHG) reports `scrape_failed_file_not_found` until they file a real Liste. For parsed cap-table updates that work for any GmbH/UG, subscribe to `shareholders` instead. |
         * @enum {string}
         */
        SubscriptionType: "details" | "history" | "shareholders" | "ubo" | "doc_articles_of_association" | "doc_company_registration" | "doc_model_protocol" | "doc_shareholder_list";
        /**
         * TenderRole
         * @description Which side of an EU public-procurement notice a company was on.
         *
         *     Overlapping, not exhaustive — a company can be both, and the large
         *     majority of the register is neither. Selecting both values therefore
         *     means "appears in TED at all" and is a genuine filter, where selecting
         *     every :class:`LegalStatus` is a no-op.
         * @enum {string}
         */
        TenderRole: "winner" | "buyer";
        /**
         * UboCoverage
         * @description Coverage signal for the UBO endpoint.
         */
        UboCoverage: {
            /**
             * Object
             * @description Discriminator. Always `ubo_coverage`.
             * @default ubo_coverage
             * @constant
             */
            object: "ubo_coverage";
            /**
             * Reason
             * @description Customer-safe one-sentence explanation of the coverage state when it's not the trivial `available`-with-nothing-to-explain case. For `partial`, names the top unresolved branches and their combined effective share so KYC consumers can decide whether to escalate. `null` only for the trivial `available` case.
             */
            reason?: string | null;
            /**
             * Status
             * @description Coverage status. `available`: chain fully resolved to natural persons, OR remaining unresolved branches sum to ≤25 % effective share and cannot hide a beneficial owner; `partial`: at least one unresolved branch could hide a beneficial owner — either a single unresolved org carries >25 % effective share at the root, or several sum to >25 %; `not_applicable`: legal form does not file a Gesellschafterliste; `not_filed`: filing expected but not yet received.
             * @default not_filed
             * @enum {string}
             */
            status: "available" | "partial" | "not_applicable" | "not_filed";
        };
        /**
         * UboEdge
         * @description A direct-ownership edge: ``source`` holds ``share_percent`` of ``target``.
         */
        UboEdge: {
            /**
             * Object
             * @description Discriminator. Always `ubo_edge`.
             * @default ubo_edge
             * @constant
             */
            object: "ubo_edge";
            /**
             * Share Percent
             * @description Direct share percentage in [0, 100] from source to target, or `null` when the underlying filing didn't report a percentage.
             */
            share_percent?: number | null;
            /**
             * Source Id
             * @description `UboNode.id` of the shareholder (the owner).
             */
            source_id: string;
            /**
             * Target Id
             * @description `UboNode.id` of the company being owned.
             */
            target_id: string;
        };
        /**
         * UboGraph
         * @description The full ownership graph rooted at the requested company.
         */
        UboGraph: {
            /**
             * Edges
             * @description Directed `source → target` ownership edges.
             */
            edges?: components["schemas"]["UboEdge"][];
            /**
             * Nodes
             * @description All nodes in the graph (root included).
             */
            nodes?: components["schemas"]["UboNode"][];
            /**
             * Object
             * @description Discriminator. Always `ubo_graph`.
             * @default ubo_graph
             * @constant
             */
            object: "ubo_graph";
            /**
             * Root Id
             * @description `UboNode.id` of the root company.
             */
            root_id: string;
        };
        /**
         * UboNode
         * @description One vertex in the UBO ownership graph.
         */
        UboNode: {
            /**
             * Eu Id
             * @description firmendata company identifier when `node_type=company`, otherwise `null`.
             */
            eu_id?: string | null;
            /**
             * Id
             * @description Stable identifier for this node within the response. Use it to match the `source_id` / `target_id` references on edges.
             */
            id: string;
            /**
             * Is Root
             * @description `true` for the company the report was requested for.
             * @default false
             */
            is_root: boolean;
            /**
             * Is Unresolved
             * @description `true` when the chain could not be walked further past this node. Beneficial-owner attribution stops here, so any UBOs above this node are missing from `beneficial_owners`. Pair with `unresolved_reason` to decide whether to retry, raise the depth cap, or accept the gap.
             * @default false
             */
            is_unresolved: boolean;
            /**
             * Node Type
             * @description `company`: a German GmbH/UG whose cap table was traversed; `person`: a natural person leaf; `organization_unresolved`: an organisation that could not be followed further (foreign entity, unknown to firmendata, or no Gesellschafterliste obligation) — the chain stops here.
             * @enum {string}
             */
            node_type: "company" | "person" | "organization_unresolved";
            /**
             * Object
             * @description Discriminator. Always `ubo_node`.
             * @default ubo_node
             * @constant
             */
            object: "ubo_node";
            /** @description Organisation details. Populated for `company` and `organization_unresolved` nodes. */
            organization?: components["schemas"]["OrganizationRef"] | null;
            /** @description Natural-person details when `node_type=person`. */
            person?: components["schemas"]["Person"] | null;
            /**
             * Unresolved Reason
             * @description Why the chain terminated at this node. `null` when `is_unresolved=false`. `foreign_org`: cap-table row referenced an organisation registered outside Germany (`country_code` set and not `DE`); foreign chains are not followed. `unlisted_org`: cap-table row referenced a German organisation we couldn't match to a known `eu_id`. `cap_table_not_filed`: company is known but no Gesellschafterliste has been filed yet. `max_depth_reached`: recursion hit the depth cap; the chain may continue further upstream.
             */
            unresolved_reason?: ("foreign_org" | "unlisted_org" | "cap_table_not_filed" | "max_depth_reached") | null;
        };
        /**
         * UboReport
         * @description Response model for `GET /v1/companies/{eu_id}/ubo`.
         */
        UboReport: {
            /**
             * Beneficial Owners
             * @description Natural persons holding more than 25 % of the root, ordered by effective share descending. Empty when no person crosses the threshold or when coverage is `not_filed`/`not_applicable`. When `coverage.status == "partial"`, each listed person's `effective_share_percent` is a **lower bound** — they could also own shares hidden behind unresolved branches.
             */
            beneficial_owners?: components["schemas"]["BeneficialOwner"][];
            /** @description Coverage status for this company. */
            coverage: components["schemas"]["UboCoverage"];
            /**
             * Eu Id
             * @description firmendata company identifier of the root.
             */
            eu_id: string;
            /** @description How current the ownership chain is. `last_checked_at` is the **oldest** (`MIN`) of the per-hop shareholder-file fetch timestamps across every company in the resolved chain — the graph is only as fresh as its weakest link. `realtime_fetching_status` reports the outcome of the realtime attempt across the whole chain for this request. */
            freshness: components["schemas"]["Freshness"];
            /** @description Full ownership graph rooted at `eu_id`. Empty for `coverage.status` of `not_filed` or `not_applicable`. */
            graph: components["schemas"]["UboGraph"];
            /**
             * Has Unresolved Branches
             * @description `true` when at least one non-root node in `graph` has `is_unresolved=true`. Indicates one or more branches terminated short of natural persons (foreign holding, missing Gesellschafterliste, depth cap). Whether this compromises the answer depends on the unresolved branches' effective share — read `coverage.status` for the materiality verdict (`partial` = it could hide a UBO; `available` with `has_unresolved_branches=true` = unresolved share is below threshold and immaterial).
             * @default false
             */
            has_unresolved_branches: boolean;
            /**
             * Object
             * @description Discriminator. Always `ubo_report`.
             * @default ubo_report
             * @constant
             */
            object: "ubo_report";
            /**
             * Potential Beneficial Owners
             * @description Unresolved organisations whose summed effective share at the root crosses 25 %. The natural person(s) controlling each entity are candidate UBOs — we just couldn't trace past the entity ourselves. Sorted by effective share descending. Always empty when `coverage.status` is `available` (no organisation crossed the threshold) or `not_filed`/`not_applicable`. Same-organisation dedup across multiple chain positions is by `(name, registered_seat, country_code)`.
             */
            potential_beneficial_owners?: components["schemas"]["PotentialBeneficialOwner"][];
        };
        /**
         * WebhookTestAttempt
         * @description One outbound POST inside a webhook test run.
         *
         *     For ``delivery_scenario=success`` / ``bad_signature`` /
         *     ``stale_timestamp`` the response carries a single attempt; for
         *     ``retry_delivery`` it carries two with byte-identical headers and
         *     body so the customer can see exactly what duplicate delivery looks
         *     like on the wire.
         */
        WebhookTestAttempt: {
            /**
             * Attempt
             * @description 1-indexed attempt number within this test run.
             */
            attempt: number;
            /**
             * Duration Ms
             * @description Wall-clock milliseconds for this attempt.
             */
            duration_ms: number;
            /**
             * Error
             * @description Short error string when the attempt did not succeed.
             */
            error?: string | null;
            /**
             * Http Status
             * @description HTTP status returned by the receiver, or `null` on transport failure.
             */
            http_status?: number | null;
            /**
             * Object
             * @description Discriminator. Always `webhook_test_attempt`.
             * @default webhook_test_attempt
             * @constant
             */
            object: "webhook_test_attempt";
            /**
             * Response Excerpt
             * @description First ~512 chars of the receiver's response body.
             */
            response_excerpt?: string | null;
            /**
             * Sent Signature Header
             * @description `X-Firmendata-Signature` header value sent on this attempt.
             */
            sent_signature_header: string;
            /**
             * Sent Timestamp Header
             * @description `X-Firmendata-Timestamp` header value sent on this attempt.
             */
            sent_timestamp_header: string;
            /**
             * Succeeded
             * @description True iff the receiver returned a 2xx status.
             */
            succeeded: boolean;
        };
        /**
         * WebhookTestDeliveryScenario
         * @description Transport-level behaviour the test endpoint should exhibit.
         *
         *     Independent of :attr:`SubscriptionTestRequest.scenario`, which picks
         *     the *kind* of notification (today only ``change``). This field
         *     picks how that notification is *delivered* so receivers can
         *     integration-test their HMAC, replay-window, and idempotent-dedupe
         *     paths against firmendata's real wire format.
         * @enum {string}
         */
        WebhookTestDeliveryScenario: "success" | "retry_delivery" | "bad_signature" | "stale_timestamp";
        /**
         * WebsiteEntry
         * @description One of a company's websites. Companies can operate several domains.
         */
        WebsiteEntry: {
            /**
             * Domain
             * @description Registrable domain.
             */
            domain: string;
            /**
             * Impressum Url
             * @description URL of the site's Impressum page.
             */
            impressum_url?: string | null;
            /**
             * Is Primary
             * @description Whether this is the company's primary website.
             * @default false
             */
            is_primary: boolean;
            /**
             * Object
             * @description Discriminator. Always `company_website`.
             * @default company_website
             * @constant
             */
            object: "company_website";
            /**
             * Verification
             * @description How the company↔website link was proven: `register` (register code found in the Impressum), `lei`, `linkedin_domain` (LinkedIn declares the domain), `name_unique`, or `unverified`.
             * @default unverified
             */
            verification: string;
        };
        /**
         * Wz2025Score
         * @description A WZ 2025 classification candidate with a confidence score.
         */
        Wz2025Score: {
            /**
             * Code
             * @description WZ 2025 code (German *Klassifikation der Wirtschaftszweige 2025*, compatible with NACE Rev. 2.1). Dotted-hierarchy format, e.g. `62.01.9`.
             * @example 62.01.9
             */
            code: string;
            /**
             * Description
             * @description Human-readable description of the WZ 2025 code.
             */
            description?: string | null;
            /**
             * Object
             * @description Discriminator. Always `wz_2025_score`.
             * @default wz_2025_score
             * @constant
             */
            object: "wz_2025_score";
            /**
             * Score
             * @description Confidence score in [0, 1] for this classification.
             */
            score?: number | null;
            /**
             * Section
             * @description WZ 2025 section letter (e.g. `J` for Information and communication).
             */
            section?: string | null;
        };
    };
    responses: never;
    parameters: never;
    requestBodies: never;
    headers: never;
    pathItems: never;
}
export type $defs = Record<string, never>;
export interface operations {
    autocompleteCompanies: {
        parameters: {
            query: {
                /** @description Free-text fragment of a company name. Minimum 3 characters after trimming. */
                q: string;
                /** @description Maximum number of suggestions to return. Hard upper bound is 25. */
                limit?: number;
                /** @description When `true`, search the German company registers live for the keyword first and index any hits not yet known to firmendata, so newly founded companies appear in the suggestions of this same response. Adds upstream latency (typically a few seconds) and a higher credit cost. Scraping failures never fail the request — `freshness.realtime_fetching_status` reports the outcome and the local suggestions are returned regardless. */
                fetch_realtime?: boolean;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Suggestion hits ordered by match relevance. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["AutocompleteResponse"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    searchCompanies: {
        parameters: {
            query?: {
                /** @description Unified keyword, tried as an eu_id, then a company-name prefix, then a person name — falling through whenever a phase returns nothing. Overrides `company_name`, `eu_id` and `person_name` when supplied alongside them. */
                q?: string | null;
                /** @description Company name prefix, matched against the folded legal name with quotes and invisible characters stripped — `Müller "Süd" GmbH` and `Müller Süd GmbH` hit the same row. The legal-form suffix is kept, so `Bosch GmbH` narrows against `Bosch`. */
                company_name?: string | null;
                /** @description Exact eu_id lookup (no prefix expansion). */
                eu_id?: string | null;
                /** @description Maximum number of hits to return per page. Hard upper bound is 50. */
                limit?: number;
                /** @description Opaque pagination cursor. Pass the `pagination.next_cursor` from a previous response to fetch the next page. */
                cursor?: string | null;
                /** @description Field to order results by. Defaults to keyword-match relevance when `q` is given, otherwise `revenue` descending. Companies missing the value always sort last, whichever direction is chosen. `total_assets` ranks far more companies than `revenue` does — see the filter notes on those two. */
                sort?: components["schemas"]["Sort"] | null;
                /** @description Sort direction. Defaults to `desc`. */
                sort_direction?: components["schemas"]["SortDirection"] | null;
                /** @description Exact Registernummer, digits only (`12345` for HRB 12345). Register numbers repeat across courts and types — combine with `register_court` / `register_type` to identify one company. */
                register_number?: number | null;
                /** @description Register type. Single-valued. */
                register_type?: components["schemas"]["RegisterType"] | null;
                /** @description Registering court (Registergericht), by its canonical Handelsregister name. An `Amtsgericht ` prefix is accepted and ignored. Single-valued. Ten listed courts were consolidated away years ago and hold no companies — they stay valid inputs so existing calls keep working. */
                register_court?: components["schemas"]["RegisterCourt"] | null;
                /** @description Registered seat city, matched exactly but umlaut-insensitively. Multi-valued, and OR-merged with `bundesland` into one location filter. */
                city?: string[] | null;
                /** @description German federal state. Multi-valued; also accepts postal codes (`BY`, `NRW`) and English names (`Bavaria`). */
                bundesland?: components["schemas"]["Bundesland"][] | null;
                /** @description Earliest founding date (inclusive, `YYYY-MM-DD`). */
                founded_from?: string | null;
                /** @description Latest founding date (inclusive, `YYYY-MM-DD`). */
                founded_to?: string | null;
                /** @description Minimum latest reported revenue in EUR, inclusive. Most German companies file abridged accounts with no profit-and-loss statement, so revenue is the scarcest financial figure — prefer `total_assets_min` to filter on size. */
                revenue_min?: number | null;
                /** @description Maximum latest reported revenue in EUR, inclusive. */
                revenue_max?: number | null;
                /** @description Minimum latest reported profit in EUR, inclusive. */
                profit_min?: number | null;
                /** @description Maximum latest reported profit in EUR, inclusive. */
                profit_max?: number | null;
                /** @description Minimum balance-sheet total (Bilanzsumme) in EUR, inclusive. Every filing company publishes a balance sheet, so this is the widest-reaching size filter. */
                total_assets_min?: number | null;
                /** @description Maximum balance-sheet total (Bilanzsumme) in EUR, inclusive. */
                total_assets_max?: number | null;
                /** @description Minimum reported employee count, inclusive. Reporting headcount is optional for most German legal forms, so this is the narrowest of the size filters — expect small result sets. */
                employee_count_min?: number | null;
                /** @description Maximum reported employee count, inclusive. */
                employee_count_max?: number | null;
                /**
                 * @description WZ 2025 industry code (German *Klassifikation der Wirtschaftszweige 2025*, compatible with NACE Rev. 2.1), at any level of the hierarchy: `K` (section), `62` (division), `62.1` (group), `62.10` (class) or `62.10.3` (subclass). Matches the company's primary code and all its parent levels. Multi-valued, OR-merged with `industry_slug`.
                 *
                 *     **Codes are WZ 2025, not WZ 2008** — the two are not interchangeable and a WZ 2008 code usually matches nothing. Classes were renumbered (`62.01` became `62.10`) and the section letters were reassigned, so WZ 2008's `K` (finance) is now `L`, and `K` is IT and telecoms.
                 */
                wz_2025_code?: string[] | null;
                /** @description High-level firmendata industry category. Expands server-side to WZ 2025 divisions. Multi-valued, OR-merged with `wz_2025_code`. */
                industry_slug?: components["schemas"]["IndustrySlug"][] | null;
                /** @description German legal form. Multi-valued. Common abbreviations and the long forms are accepted too (`eK`, `UG (haftungsbeschränkt)`, `GmbH & Co KG`). */
                rechtsform?: components["schemas"]["Rechtsform"][] | null;
                /** @description Name of an associated person — current or historical board member, shareholder or partner. Combines with the other filters, which narrow the companies that person is linked to. */
                person_name?: string | null;
                /** @description CPV (Common Procurement Vocabulary) prefix from the EU tenders a company won — `45` matches all construction contracts. */
                cpv_code?: string | null;
                /** @description Minimum average birth year across the company's associated persons. */
                avg_birth_year_min?: number | null;
                /** @description Maximum average birth year across the company's associated persons. */
                avg_birth_year_max?: number | null;
                /**
                 * @description Company lifecycle status, resolved from the merged Handelsregister (SI + CD) and Insolvenzbekanntmachungen timelines. Multi-valued.
                 *
                 *     - `active` — no insolvency or dissolution on record
                 *     - `insolvent` — proceedings open or preliminary measures in force
                 *     - `in_liquidation` — dissolved, with a Liquidator in office
                 *     - `dissolved` — dissolved with no Liquidator, or a proceeding since lifted
                 *     - `deleted` — removed from the register; outranks the others
                 *
                 *     The five are disjoint and exhaustive, so selecting every value is the same as selecting none.
                 */
                legal_status?: components["schemas"]["LegalStatus"][] | null;
                /**
                 * @description Statutory size class under § 267 HGB. Multi-valued.
                 *
                 *     - `klein` — kleine Kapitalgesellschaft (§ 267 Abs. 1)
                 *     - `mittelgross` — mittelgroße Kapitalgesellschaft (§ 267 Abs. 2)
                 *
                 *     Read from the form of the filing itself: a company that omits its profit-and-loss statement (§ 326 Abs. 1) or collapses it into a Rohergebnis (§ 276) is claiming a relief only a company of that size may claim. Publishing *more* than required is permitted at every size and asserts nothing, so companies that file a full P&L carry no class — for those the reported `revenue` is the answer. The values are therefore not exhaustive, and selecting both is a real filter meaning "the filing form implies a size class", not a no-op the way selecting every `legal_status` is. `gross` is not offered because no relief can assert largeness: the company that claims nothing is exactly the large one.
                 *
                 *     Because the § 267 test is "does not exceed at least two of three" (balance-sheet total, revenue, employees), a class on its own does not bound revenue — it does so only where a second criterion is already over its limit. This filter is a size filter, not a revenue filter.
                 */
                company_size?: components["schemas"]["CompanySize"][] | null;
                /**
                 * @description Companies listed in at least one of these external registries. Multi-valued.
                 *
                 *     - `Steuerberaterkammer` — BStBK directory
                 *     - `Wirtschaftsprueferkammer` — WPK directory
                 *     - `bbh` — Bundesverband der Bilanzbuchhalter
                 *     - `BaFin` — supervised institutes
                 *     - `LEI` — holds an active Legal Entity Identifier
                 */
                membership?: components["schemas"]["Membership"][] | null;
                /** @description Exact LEI (ISO 17442), matched against the company's active LEI record. Case-insensitive. */
                lei_code?: string | null;
                /** @description Exact USt-IdNr (German / EU VAT number) as published on the company's Impressum. Case-insensitive. */
                ust_idnr?: string | null;
                /** @description Restrict to companies with (`true`) or without (`false`) a known website. */
                has_website?: boolean | null;
                /** @description Restrict to companies with (`true`) or without (`false`) a known email. */
                has_email?: boolean | null;
                /** @description Also search the German registers live for `q` / `company_name` (one is required with this flag) and append any companies not yet indexed, carrying identity and seat only. First page only. Adds a few seconds of latency and costs more credits; scraping failures never fail the request — check `freshness.realtime_fetching_status`. */
                fetch_realtime?: boolean;
                /**
                 * @description Companies appearing in EU public-procurement notices (TED), in the selected role. Multi-valued.
                 *
                 *     - `winner` — was awarded a public contract
                 *     - `buyer` — published a notice as the contracting authority
                 *
                 *     The roles overlap and are *not* exhaustive — most of the register appears in neither — so unlike `legal_status`, selecting both is a real filter meaning "appears in TED at all". Coverage starts with notices published 2025-01-02. `buyer` is intrinsically small: most contracting authorities have no Handelsregister entry to match against.
                 */
                tender_role?: components["schemas"]["TenderRole"][] | null;
                /**
                 * @description Restrict to companies where two or more members of one family hold an ownership or governing role (`true`), or where the people on record are unrelated (`false`).
                 *
                 *     Derived from the register: shared surnames among owners and officers, a founder's surname carried in the company name, family wording in the name (`Gebr.`, `& Söhne`), a generation gap between two relatives, or a shared address. Companies whose register entry shows only one person and whose name says nothing are reported as neither — they are excluded from both `true` and `false` rather than assumed unrelated. Spouses who took different surnames are not visible to this filter, so it under-reports rather than over-claims.
                 */
                family_managed?: boolean | null;
                /**
                 * @description Restrict to companies where a person holding an ownership or governing role at first registration is still in office (`true`), or where the founding officers have all been replaced (`false`).
                 *
                 *     Requires the chronological register extract, so companies whose history has not been parsed are excluded from both values. Companies whose extract begins with a transfer from another register court are also excluded: their first entry records the move, not the founding.
                 */
                founder_led?: boolean | null;
                /** @description Restrict to founder-led companies whose oldest serving founder has reached 65 (`true`), or has not (`false`). Companies that are not founder-led, and founders with no birth date on record, are excluded from both values. */
                succession_risk?: boolean | null;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Page of search hits with pagination, echoed filters, and execution time. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SearchResponse"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getCompanyDetail: {
        parameters: {
            query?: {
                /** @description When `true`, refresh the company's register snapshot from the German registries before building the response, so the underlying data is as fresh as possible. Adds upstream latency (typically several seconds). Scraping failures are logged but do not fail the request — the most recent indexed data is returned instead. The chronological register history is refreshed by `/v1/companies/{eu_id}/history` instead — this endpoint does not refresh it. */
                fetch_realtime?: boolean;
            };
            header?: never;
            path: {
                /** @description firmendata company identifier (also referred to as `eu_id`). */
                eu_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The company's detail profile. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompanyDetail"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    downloadCompanyDocument: {
        parameters: {
            query: {
                /** @description Document type to download. */
                file_type: "register_extract_current" | "register_extract_chronological" | "shareholder_list" | "articles_of_association" | "company_registration" | "model_protocol";
                /** @description Specific document id (as returned in the company detail `documents[]` block). Defaults to the latest published file of `file_type` when omitted. */
                file_id?: string | null;
                /** @description When `true`, fetch the requested `file_type` from the German registries before resolving the file, so the presigned URL points at the freshest version. Adds upstream latency (typically several seconds). Failures are surfaced via `freshness.realtime_fetching_status` rather than failing the request. */
                fetch_realtime?: boolean;
            };
            header?: never;
            path: {
                /** @description firmendata company identifier (also referred to as `eu_id`). */
                eu_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Document metadata plus a 1-day presigned download URL. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompanyDocumentDownload"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getCompanyFinancials: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                /** @description firmendata company identifier (also referred to as `eu_id`). */
                eu_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Aggregated and historic financial data for the company. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompanyFinancials"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getCompanyHistory: {
        parameters: {
            query?: {
                /** @description When `true`, refresh the chronological register history from the German registries before responding. Adds upstream latency (typically several seconds). Scraping failures are surfaced via `freshness.realtime_fetching_status` rather than failing the request. */
                fetch_realtime?: boolean;
            };
            header?: never;
            path: {
                /** @description firmendata company identifier (also referred to as `eu_id`). */
                eu_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Chronological register history grouped by category. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompanyHistory"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getCompanyShareholders: {
        parameters: {
            query?: {
                /** @description When `true`, scrape the Liste der Gesellschafter from the German registries and re-parse it before responding. Adds upstream latency (typically several seconds). Scraping failures are surfaced via `freshness.realtime_fetching_status` rather than failing the request. */
                fetch_realtime?: boolean;
            };
            header?: never;
            path: {
                /** @description firmendata company identifier (also referred to as `eu_id`). */
                eu_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Current cap table with coverage status. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["ShareholdersReport"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getCompanyUbo: {
        parameters: {
            query?: {
                /** @description When `true`, refetch the latest Liste der Gesellschafter for every German GmbH/UG visited in the ownership chain before building the graph. Adds noticeable latency. Failures are surfaced via `freshness.realtime_fetching_status` rather than failing the request. */
                fetch_realtime?: boolean;
            };
            header?: never;
            path: {
                /** @description firmendata company identifier (also referred to as `eu_id`). */
                eu_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description UBO graph plus beneficial-owner roll-up. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["UboReport"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getLists: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description The caller's lists. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompanyListsResponse"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getList: {
        parameters: {
            query?: {
                /** @description Companies per page. */
                limit?: number;
                /** @description Opaque pagination cursor. Pass the `pagination.next_cursor` from a previous response to fetch the next page. */
                cursor?: string | null;
                /** @description Order the list by a column instead of by when companies were added. Same vocabulary as `/companies/search`. */
                sort?: components["schemas"]["Sort"] | null;
                /** @description Sort direction; applies only with `sort`. */
                sort_direction?: components["schemas"]["SortDirection"];
            };
            header?: never;
            path: {
                /** @description Identifier of one of your lists, from `GET /v1/lists`. */
                list_id: number;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description One page of the list's companies. */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["CompanyListDetailResponse"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    listSubscriptions: {
        parameters: {
            query?: {
                /** @description Filter to subscriptions for one company. */
                eu_id?: string | null;
                /** @description Filter by subscription type. */
                subscription_type?: components["schemas"]["SubscriptionType"] | null;
                /** @description Filter by `recurring` or `one_time`. */
                mode?: components["schemas"]["SubscriptionMode"] | null;
                limit?: number;
                offset?: number;
            };
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionList"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    createSubscription: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SubscriptionCreateRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            201: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionCreated"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The request conflicts with the current resource state (e.g. duplicate subscription). */
            409: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A subscription for this company, type, and notification channel already exists.",
                     *       "instance": "/v1/subscriptions",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 409,
                     *       "title": "Conflict.",
                     *       "type": "https://api.firmendata.com/problems/conflict"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getEvent: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                event_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionEvent"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    resendEvent: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                event_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionEventResendResponse"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    testDelivery: {
        parameters: {
            query?: never;
            header?: never;
            path?: never;
            cookie?: never;
        };
        requestBody: {
            content: {
                "application/json": components["schemas"]["SubscriptionTestRequest"];
            };
        };
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionTestResponse"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    getSubscription: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                subscription_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["Subscription"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    deleteSubscription: {
        parameters: {
            query?: never;
            header?: never;
            path: {
                subscription_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            204: {
                headers: {
                    [name: string]: unknown;
                };
                content?: never;
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
    listEvents: {
        parameters: {
            query?: {
                limit?: number;
                offset?: number;
            };
            header?: never;
            path: {
                subscription_id: string;
            };
            cookie?: never;
        };
        requestBody?: never;
        responses: {
            /** @description Successful Response */
            200: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    "application/json": components["schemas"]["SubscriptionEventList"];
                };
            };
            /** @description Missing, invalid, revoked, or expired bearer token. */
            401: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "A valid `Authorization: Bearer <token>` header is required.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 401,
                     *       "title": "Authentication required.",
                     *       "type": "https://api.firmendata.com/problems/unauthenticated"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description The requested resource does not exist. */
            404: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "No company with id DEF1103R.HRB999999B.",
                     *       "instance": "/v1/companies/DEF1103R.HRB999999B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 404,
                     *       "title": "Resource not found.",
                     *       "type": "https://api.firmendata.com/problems/not-found"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Request validation failed; see `errors[]` for per-field details. */
            422: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "One or more query parameters are invalid.",
                     *       "errors": [
                     *         {
                     *           "code": "less_than_or_equal",
                     *           "path": "limit"
                     *         },
                     *         {
                     *           "code": "enum_mismatch",
                     *           "path": "wz_2025_code"
                     *         }
                     *       ],
                     *       "instance": "/v1/companies/search",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 422,
                     *       "title": "Request validation failed.",
                     *       "type": "https://api.firmendata.com/problems/validation-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Rate limit exceeded. Rejected requests do not consume the limit or any credits. Honour `Retry-After` (seconds) before retrying. */
            429: {
                headers: {
                    /** @description Numeric limit of the tighter window currently applying. */
                    "RateLimit-Limit"?: number;
                    /** @description Requests left in that window (0 on 429). */
                    "RateLimit-Remaining"?: number;
                    /** @description Seconds until the window resets. */
                    "RateLimit-Reset"?: number;
                    /** @description Seconds to wait before the next request will succeed. */
                    "Retry-After"?: number;
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "Rate limit exceeded: more than 5 requests per 1 second. Retry in 1s.",
                     *       "instance": "/v1/companies/autocomplete",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 429,
                     *       "title": "Too many requests.",
                     *       "type": "https://api.firmendata.com/problems/rate-limit-exceeded"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
            /** @description Unexpected server error. */
            500: {
                headers: {
                    [name: string]: unknown;
                };
                content: {
                    /**
                     * @example {
                     *       "detail": "An unexpected error occurred. Please retry later or contact support with the `request_id`.",
                     *       "instance": "/v1/companies/DEF1103R.HRB279792B",
                     *       "request_id": "req_01HXYZABC123DEF456GHI789J",
                     *       "status": 500,
                     *       "title": "Internal server error.",
                     *       "type": "https://api.firmendata.com/problems/server-error"
                     *     }
                     */
                    "application/problem+json": components["schemas"]["Problem"];
                };
            };
        };
    };
}
